/*
 * lamo_runtime_data.c - AUTO-GENERATED. Do not edit by hand.
 *
 * Regenerate with: python3 scripts/embed_runtime.py
 *
 * Contains the Lamo runtime (from lamo_runtime.h) as an escaped C
 * string. The codegen emits this verbatim at the top of every
 * generated .c program, replacing ~600 lines of fprintf() calls.
 */

#include "lamo_runtime_data.h"

const char lamo_runtime_source[] =
    "#ifndef LAMO_RUNTIME_H\n#define LAMO_RUNTIME_H\n\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include"
    " <math.h>   /* fmod() \20024 usado por lamo_mod() para operandos float */\n\n#if defined(__GNUC__) || defined(__clang__)"
    "\n#define LAMO_UNUSED __attribute__((unused))\n#else\n#define LAMO_UNUSED\n#endif\n\n/* --------------------------------"
    "---------------------------------- */\n/* 1. Value runtime (always present)                                  */\n/* ----"
    "-------------------------------------------------------------- */\n\ntypedef enum {\n    LAMO_VALUE_INT,\n    LAMO_VALUE"
    "_FLOAT,\n    LAMO_VALUE_STRING,\n    LAMO_VALUE_BOOL,\n    LAMO_VALUE_ARRAY,  /* Sprint 3: [1, 2, 3] - array of LamoValu"
    "es */\n    /* Phase 2: a struct value is stored as a LamoArray under the hood -\n     * field 0 is at items[0], field 1 "
    "at items[1], etc. We use a separate\n     * type tag so the printer can render it differently and so the\n     * semanti"
    "c/codegen can tell struct-typed values from raw arrays at\n     * runtime (useful for debugging and for printing). */\n "
    "   LAMO_VALUE_STRUCT\n} LamoValueType;\n\n/* Sprint 3: forward-declare LamoArray so we can reference it from inside\n * "
    "the LamoValue struct. The full definition appears after LamoValue. */\nstruct LamoArray;\n\ntypedef struct {\n    LamoVa"
    "lueType type;\n    long long int_value;\n    double float_value;\n    const char* string_value;\n    /* Sprint 3: only s"
    "et when type == LAMO_VALUE_ARRAY. Pointer to a\n     * heap-allocated LamoArray; the array is owned by the arena. */\n  "
    "  struct LamoArray* array_value;\n} LamoValue;\n\n/* Sprint 3: LamoArray \20024 a growable array of LamoValues.\n *\n * "
    "Memory model: arrays are heap-allocated and tracked in the string\n * arena (lamo_string_arena). They're freed at progra"
    "m exit via the\n * atexit handler. We reuse the arena rather than introducing a\n * separate arena because:\n *   - the "
    "arena already exists and works\n *   - arrays and strings have the same lifetime (program exit)\n *   - long-running pro"
    "grams already have a documented arena-growth\n *     limitation; arrays inherit that same limitation\n *\n * Each LamoAr"
    "ray owns its `items` buffer. Pushing may realloc the\n * buffer; the array struct itself is fixed-location once allocate"
    "d\n * (so LamoValue.array_value pointers stay valid). */\ntypedef struct LamoArray {\n    LamoValue* items;\n    long lo"
    "ng count;\n    long long capacity;\n} LamoArray;\n\nstatic LAMO_UNUSED LamoValue lamo_make_int(long long value) {\n    L"
    "amoValue result;\n    result.type = LAMO_VALUE_INT;\n    result.int_value = value;\n    result.float_value = 0.0;\n    r"
    "esult.string_value = NULL;\n    result.array_value = NULL;\n    return result;\n}\n\nstatic LAMO_UNUSED LamoValue lamo_m"
    "ake_float(double value) {\n    LamoValue result;\n    result.type = LAMO_VALUE_FLOAT;\n    result.int_value = 0;\n    re"
    "sult.float_value = value;\n    result.string_value = NULL;\n    result.array_value = NULL;\n    return result;\n}\n\nsta"
    "tic LAMO_UNUSED LamoValue lamo_make_bool(int value) {\n    LamoValue result;\n    result.type = LAMO_VALUE_BOOL;\n    re"
    "sult.int_value = value ? 1 : 0;\n    result.float_value = 0.0;\n    result.string_value = NULL;\n    result.array_value "
    "= NULL;\n    return result;\n}\n\nstatic LAMO_UNUSED LamoValue lamo_make_string(const char* value) {\n    LamoValue resu"
    "lt;\n    result.type = LAMO_VALUE_STRING;\n    result.int_value = 0;\n    result.float_value = 0.0;\n    result.string_v"
    "alue = value ? value : \"\";\n    result.array_value = NULL;\n    return result;\n}\n\n/* Sprint 3: make an array value "
    "from an existing LamoArray pointer.\n * The LamoArray must already be heap-allocated (lamo_array_alloc).\n * The LamoVal"
    "ue does NOT take ownership - the arena tracks the array\n * and frees it at exit. */\nstatic LAMO_UNUSED LamoValue lamo_"
    "make_array(LamoArray* array) {\n    LamoValue result;\n    result.type = LAMO_VALUE_ARRAY;\n    result.int_value = 0;\n "
    "   result.float_value = 0.0;\n    result.string_value = NULL;\n    result.array_value = array;\n    return result;\n}\n"
    "\n/* Phase 2: make a struct value. Structs reuse the LamoArray layout -\n * each field occupies one slot in items[]. The"
    " distinct type tag lets\n * the printer render `Player { name: \"x\", hp: 10 }` differently from\n * `[ \"x\", 10 ]`, bu"
    "t the memory model is identical. */\nstatic LAMO_UNUSED LamoValue lamo_make_struct(LamoArray* array) {\n    LamoValue re"
    "sult;\n    result.type = LAMO_VALUE_STRUCT;\n    result.int_value = 0;\n    result.float_value = 0.0;\n    result.string"
    "_value = NULL;\n    result.array_value = array;\n    return result;\n}\n\n/* ---- String arena -------------------------"
    "-------------------------\n * The Lamo runtime has no GC and no per-LamoValue ownership tracking.\n * Strings allocated "
    "dynamically (concatenation, input_str, ...) are\n * registered in this arena and freed all at once on program exit via\n"
    " * atexit(). This fixes the classic leak in loops that concatenate:\n * before, each `s = s + \"x\"` leaked the previous"
    " buffer; now every\n * buffer is tracked and freed on shutdown.\n *\n * Sprint 3: the arena now holds void* (not just ch"
    "ar*) so it can also\n * track LamoArray structs and their internal `items` buffers. All\n * allocations share the same l"
    "ifetime (program exit), so one arena is\n * enough.\n *\n * GC rollout Step 2: each arena allocation now also gets a Lam"
    "oGcHeader\n * prepended (see the GC section below). The arena stores the PAYLOAD\n * pointer (what user code sees); the "
    "GC tracks the HEADER pointer. Both\n * are freed at exit by lamo_arena_free_all (which walks the arena and\n * frees eac"
    "h non-NULL payload's underlying header).\n *\n * Known limitation: long-running programs (e.g. HTTP servers) see the\n *"
    " arena grow while they run. This is now fixable by opting into the\n * mark-sweep GC (see the GC section below and docs/"
    "MEMORY-MODEL.md).\n */\n\n/* Forward declarations for the GC machinery (defined below). The arena\n * allocator hooks in"
    "to the GC heap list, so we need these visible here.\n * The root stack variables are also referenced by lamo_arena_free_"
    "all\n * (it frees the root stack at exit), so they're forward-declared too. */\ntypedef struct LamoGcHeader LamoGcHeader"
    ";\nstruct LamoGcHeader {\n    size_t size;\n    unsigned char mark : 1;\n    unsigned char is_array : 1;\n    unsigned c"
    "har in_use : 1;\n    struct LamoGcHeader* next;\n};\nstatic LamoGcHeader* lamo_gc_heap_head;\nstatic size_t lamo_gc_byte"
    "s_since_collect;\nstatic size_t lamo_gc_threshold;\nstatic size_t lamo_gc_live_bytes;\nstatic size_t lamo_gc_live_count;"
    "\nstatic LamoValue** lamo_gc_root_stack;\nstatic size_t lamo_gc_root_stack_count;\nstatic size_t lamo_gc_root_stack_capa"
    "city;\nstatic void lamo_gc_collect(void);\nstatic LamoGcHeader* lamo_gc_header_for(const void* payload);\n\nstatic void*"
    "* lamo_string_arena = NULL;\nstatic size_t lamo_string_arena_count = 0;\nstatic size_t lamo_string_arena_capacity = 0;\n"
    "\nstatic LAMO_UNUSED void* lamo_arena_alloc(size_t length) {\n    void* block;\n    LamoGcHeader* header;\n    size_t to"
    "tal = length + sizeof(LamoGcHeader);\n    if (lamo_string_arena_count == lamo_string_arena_capacity) {\n        size_t n"
    "ew_cap = lamo_string_arena_capacity ? lamo_string_arena_capacity * 2 : 16;\n        void** new_arena = realloc(lamo_stri"
    "ng_arena, sizeof(void*) * new_cap);\n        if (!new_arena) { fprintf(stderr, \"runtime error: string arena exhausted"
    "\\n\"); exit(1); }\n        lamo_string_arena = new_arena;\n        lamo_string_arena_capacity = new_cap;\n    }\n    he"
    "ader = (LamoGcHeader*)malloc(total);\n    if (!header) { fprintf(stderr, \"runtime error: failed to allocate string\\n\""
    "); exit(1); }\n    header->size = length;\n    header->mark = 0;\n    header->is_array = 0;  /* default: opaque buffer ("
    "string bytes, items buffer) */\n    header->in_use = 1;\n    header->next = lamo_gc_heap_head;\n    lamo_gc_heap_head = "
    "header;\n    block = (char*)header + sizeof(LamoGcHeader);\n    lamo_string_arena[lamo_string_arena_count++] = block;\n"
    "\n    /* Auto-trigger GC if threshold is set and we've crossed it. */\n    lamo_gc_bytes_since_collect += length + sizeo"
    "f(LamoGcHeader);\n    if (lamo_gc_threshold > 0 && lamo_gc_bytes_since_collect >= lamo_gc_threshold) {\n        lamo_gc_"
    "collect();\n    }\n    return block;\n}\n\n/* Like lamo_arena_alloc, but marks the allocation as a traceable LamoArray\n"
    " * struct (so the GC will walk its items[] during the mark phase). Used by\n * lamo_array_alloc for the LamoArray itself"
    "; the items buffer is allocated\n * with the plain lamo_arena_alloc (it's reached via the parent's trace). */\nstatic LA"
    "MO_UNUSED void* lamo_arena_alloc_traced(size_t length) {\n    void* block = lamo_arena_alloc(length);\n    if (block) {"
    "\n        LamoGcHeader* h = lamo_gc_header_for(block);\n        if (h) h->is_array = 1;\n    }\n    return block;\n}\n\n"
    "static LAMO_UNUSED char* lamo_arena_strdup(const char* value) {\n    size_t length;\n    char* copy;\n    if (!value) { "
    "value = \"\"; }\n    length = strlen(value);\n    copy = (char*)lamo_arena_alloc(length + 1);\n    memcpy(copy, value, l"
    "ength + 1);\n    return copy;\n}\n\nstatic LAMO_UNUSED void lamo_arena_free_all(void) {\n    size_t i;\n    for (i = 0; "
    "i < lamo_string_arena_count; i++) {\n        /* Sprint 3: arena entries may be char* (string buffers), LamoArray*\n     "
    "    * (array structs), or LamoValue* (array items buffers). We can't\n         * tell them apart at free time, but free("
    ") works on all of them.\n         *\n         * GC Step 2: arena entries are PAYLOAD pointers (the user-visible\n       "
    "  * allocation). The actual malloc'd block starts at\n         * `payload - sizeof(LamoGcHeader)`. NULL entries are slot"
    "s that\n         * lamo_gc_collect already freed during the run; free(NULL) is safe. */\n        void* payload = lamo_st"
    "ring_arena[i];\n        if (payload) {\n            LamoGcHeader* h = (LamoGcHeader*)((char*)payload - sizeof(LamoGcHead"
    "er));\n            free(h);\n        }\n    }\n    free(lamo_string_arena);\n    lamo_string_arena = NULL;\n    lamo_str"
    "ing_arena_count = 0;\n    lamo_string_arena_capacity = 0;\n    /* GC heap list is also gone now (every header was freed "
    "above). Reset\n     * the head pointer so a subsequent gc_collect (which shouldn't happen\n     * after atexit, but defe"
    "nsive) doesn't walk freed memory. */\n    lamo_gc_heap_head = NULL;\n    /* Free the root stack too \20024 it's the only"
    " GC allocation that didn't\n     * go through lamo_arena_alloc. */\n    free(lamo_gc_root_stack);\n    lamo_gc_root_stac"
    "k = NULL;\n    lamo_gc_root_stack_count = 0;\n    lamo_gc_root_stack_capacity = 0;\n}\n\n/* ============================"
    "====================================== */\n/* GC (mark-sweep, opt-in) \20024 MEMORY-MODEL.md Step 2                    *"
    "/\n/* ================================================================== */\n/*\n * Every allocation made by `lamo_arena"
    "_alloc` is now also tracked in an\n * intrusive singly-linked list (`lamo_gc_heap_head`) via a small header\n * prepende"
    "d to each block. The header type, the GC state globals, and\n * the root stack variables are forward-declared near the a"
    "rena (above)\n * so that lamo_arena_alloc and lamo_arena_free_all can hook into them.\n * The full implementation of mar"
    "k/sweep lives here.\n *\n * Backward compatibility: programs that never call `gc_collect()` or\n * `gc_set_threshold(N >"
    " 0)` see zero GC overhead. The only cost is the\n * per-allocation header (16 bytes typical) and one pointer write per\n"
    " * allocation (linking into the heap list). Allocations are still freed\n * in bulk at exit by `lamo_arena_free_all`, wh"
    "ich now also walks the\n * GC heap list to free headers (rather than the old arena array \20024 same\n * effect, one les"
    "s data structure).\n */\n\n/* (No static state declarations here \20024 they're forward-declared above.) */\n\n/* Return"
    "s the header that precedes a payload pointer, or NULL if the\n * pointer doesn't look like a GC-tracked allocation (e.g."
    ", it's a static\n * string literal). Used during marking to find the header to mark. */\nstatic LAMO_UNUSED LamoGcHeader"
    "* lamo_gc_header_for(const void* payload) {\n    if (!payload) return NULL;\n    /* The header sits immediately before t"
    "he payload. We don't validate\n     * that the pointer is actually in our heap (that would require a\n     * range check"
    "); callers only pass pointers they got from\n     * lamo_gc_alloc, so this is safe. */\n    return (LamoGcHeader*)((char"
    "*)payload - sizeof(LamoGcHeader));\n}\n\n/* Mark a single LamoValue as reachable, recursing into arrays/structs. */\nsta"
    "tic void lamo_gc_mark_value(LamoValue v);\n\nstatic LAMO_UNUSED void lamo_gc_mark_header(LamoGcHeader* h) {\n    if (!h "
    "|| h->mark) return;\n    h->mark = 1;\n    if (h->is_array) {\n        /* Payload is a LamoArray struct. Trace its items"
    " buffer. */\n        LamoArray* arr = (LamoArray*)((char*)h + sizeof(LamoGcHeader));\n        long long i;\n        /* F"
    "irst, mark the items buffer allocation (if any). */\n        if (arr->items) {\n            LamoGcHeader* items_h = lamo"
    "_gc_header_for(arr->items);\n            if (items_h && items_h->in_use) {\n                lamo_gc_mark_header(items_h)"
    ";\n            }\n        }\n        /* Then mark each item that's a traceable value. */\n        for (i = 0; i < arr->c"
    "ount; i++) {\n            lamo_gc_mark_value(arr->items[i]);\n        }\n    }\n    /* For opaque payloads (string bytes"
    ", items buffer), there's nothing\n     * internal to trace \20024 marking the header is enough. */\n}\n\nstatic LAMO_UNU"
    "SED void lamo_gc_mark_value(LamoValue v) {\n    if (v.type == LAMO_VALUE_STRING) {\n        if (v.string_value) {\n     "
    "       /* String literals (compiled into the binary) aren't in our heap\n             * and don't need marking. We try t"
    "o mark the header; if the\n             * pointer is to a GC-tracked buffer, the header gets marked.\n             * If "
    "it's a static literal, lamo_gc_header_for returns garbage\n             * \20024 but we only trust the result if `in_use"
    "` is set, which\n             * static memory won't be (it'll be random bytes, almost\n             * certainly not 1)."
    "\n             *\n             * To make this safe, we walk the heap list looking for a\n             * matching payload"
    " pointer. This is O(n) per string but\n             * typical heaps are small. */\n            LamoGcHeader* h = lamo_gc"
    "_heap_head;\n            while (h) {\n                if ((char*)h + sizeof(LamoGcHeader) == v.string_value) {\n        "
    "            lamo_gc_mark_header(h);\n                    return;\n                }\n                h = h->next;\n     "
    "       }\n            /* Not found in heap \20024 it's a static literal. Nothing to mark. */\n        }\n    } else if ("
    "v.type == LAMO_VALUE_ARRAY || v.type == LAMO_VALUE_STRUCT) {\n        if (v.array_value) {\n            LamoGcHeader* h "
    "= lamo_gc_heap_head;\n            while (h) {\n                if ((char*)h + sizeof(LamoGcHeader) == (char*)v.array_val"
    "ue) {\n                    lamo_gc_mark_header(h);\n                    return;\n                }\n                h = "
    "h->next;\n            }\n            /* Not found \20024 defensive, shouldn't happen for traceable values. */\n        }"
    "\n    }\n    /* INT, FLOAT, BOOL have no heap allocation to mark. */\n}\n\n/* Push a LamoValue* onto the root stack. Use"
    "d by LAMO_GC_PUSH_ROOT. */\nstatic LAMO_UNUSED void lamo_gc_push_root(LamoValue* v) {\n    if (lamo_gc_root_stack_count "
    "== lamo_gc_root_stack_capacity) {\n        size_t new_cap = lamo_gc_root_stack_capacity ? lamo_gc_root_stack_capacity * "
    "2 : 64;\n        LamoValue** new_stack = (LamoValue**)realloc(lamo_gc_root_stack, sizeof(LamoValue*) * new_cap);\n      "
    "  if (!new_stack) {\n            /* Out of memory for root stack \20024 disable GC rather than crash. */\n            la"
    "mo_gc_threshold = 0;\n            return;\n        }\n        lamo_gc_root_stack = new_stack;\n        lamo_gc_root_stac"
    "k_capacity = new_cap;\n    }\n    lamo_gc_root_stack[lamo_gc_root_stack_count++] = v;\n}\n\n/* Pop n entries from the ro"
    "ot stack. Used by LAMO_GC_POP_ROOTS_N. */\nstatic LAMO_UNUSED void lamo_gc_pop_roots_n(int n) {\n    if (n <= 0) return;"
    "\n    if ((size_t)n > lamo_gc_root_stack_count) {\n        /* Defensive: should never happen if codegen is correct. */\n"
    "        lamo_gc_root_stack_count = 0;\n        return;\n    }\n    lamo_gc_root_stack_count -= (size_t)n;\n}\n\n/* Run a"
    " full mark-sweep collection.\n *   1. Mark phase: clear all marks, then walk the root stack, marking\n *      every reac"
    "hable allocation.\n *   2. Sweep phase: walk the heap list. Any allocation with mark==0 is\n *      freed; its slot is r"
    "emoved from the list. Allocations with mark==1\n *      have their mark cleared for the next cycle.\n *\n * After sweep,"
    " we update lamo_gc_live_bytes / lamo_gc_live_count for\n * observability (tests, gc_heap_size() builtin). */\nstatic LAM"
    "O_UNUSED void lamo_gc_collect(void) {\n    LamoGcHeader* h;\n    LamoGcHeader* prev;\n    LamoGcHeader* next;\n    size_"
    "t i;\n\n    /* Clear all marks. */\n    h = lamo_gc_heap_head;\n    while (h) {\n        h->mark = 0;\n        h = h->ne"
    "xt;\n    }\n\n    /* Mark phase: walk the root stack. */\n    for (i = 0; i < lamo_gc_root_stack_count; i++) {\n        "
    "if (lamo_gc_root_stack[i]) {\n            lamo_gc_mark_value(*lamo_gc_root_stack[i]);\n        }\n    }\n\n    /* Sweep "
    "phase: walk the heap list, freeing unmarked allocations.\n     * We rebuild the list as we go (single pass with prev/nex"
    "t pointers). */\n    prev = NULL;\n    h = lamo_gc_heap_head;\n    lamo_gc_live_bytes = 0;\n    lamo_gc_live_count = 0;"
    "\n    while (h) {\n        next = h->next;\n        if (!h->mark) {\n            /* Free this allocation. Also remove it"
    "s entry from the arena\n             * array so lamo_arena_free_all doesn't double-free. We do\n             * this by s"
    "etting the arena slot to NULL (free(NULL) is safe).\n             * To find the arena slot, we search by payload pointer"
    ". */\n            char* payload = (char*)h + sizeof(LamoGcHeader);\n            size_t j;\n            for (j = 0; j < l"
    "amo_string_arena_count; j++) {\n                if (lamo_string_arena[j] == payload) {\n                    lamo_string_"
    "arena[j] = NULL;\n                    break;\n                }\n            }\n            free(h);\n            if (pr"
    "ev) prev->next = next; else lamo_gc_heap_head = next;\n        } else {\n            h->mark = 0;  /* clear for next cyc"
    "le */\n            h->in_use = 1;\n            lamo_gc_live_bytes += h->size + sizeof(LamoGcHeader);\n            lamo_g"
    "c_live_count++;\n            prev = h;\n        }\n        h = next;\n    }\n\n    lamo_gc_bytes_since_collect = 0;\n}\n"
    "\n/* Set the auto-trigger threshold (bytes). 0 disables auto-trigger. */\nstatic LAMO_UNUSED void lamo_gc_set_threshold("
    "size_t bytes) {\n    lamo_gc_threshold = bytes;\n}\n\n/* Returns the current live heap size in bytes (after the last col"
    "lect). */\nstatic LAMO_UNUSED long long lamo_gc_heap_size(void) {\n    return (long long)lamo_gc_live_bytes;\n}\n\n/* Re"
    "turns the current live allocation count (after the last collect). */\nstatic LAMO_UNUSED long long lamo_gc_heap_count(vo"
    "id) {\n    return (long long)lamo_gc_live_count;\n}\n\n/* Run a collection and return the resulting live allocation coun"
    "t.\n * This is the runtime entry point for the user-facing `gc_collect()`\n * builtin, which returns the live count so t"
    "ests can assert that the\n * heap shrank after dropping references. */\nstatic LAMO_UNUSED long long lamo_gc_collect_cou"
    "nt(void) {\n    lamo_gc_collect();\n    return lamo_gc_live_count;\n}\n\n/* LAMO_GC_PUSH_ROOT(&v) and LAMO_GC_POP_ROOTS_"
    "N(n) are the macros the\n * codegen emits at function entry / exit. They expand to runtime calls\n * that are essentiall"
    "y no-ops when no gc_collect() ever runs (the push\n * is one memory write, the pop is one decrement). */\n#define LAMO_G"
    "C_PUSH_ROOT(vptr) lamo_gc_push_root(vptr)\n#define LAMO_GC_POP_ROOTS_N(n)  lamo_gc_pop_roots_n(n)\n\nstatic LAMO_UNUSED "
    "char* lamo_heap_strdup(const char* value) {\n    size_t length;\n    char* copy;\n    if (!value) { value = \"\"; }\n   "
    " length = strlen(value);\n    copy = malloc(length + 1);\n    if (!copy) {\n        fprintf(stderr, \"runtime error: fai"
    "led to allocate string\\n\");\n        exit(1);\n    }\n    memcpy(copy, value, length + 1);\n    return copy;\n}\n\nsta"
    "tic LAMO_UNUSED void lamo_runtime_type_error(const char* message) {\n    fprintf(stderr, \"runtime error: %s\\n\", messa"
    "ge);\n    exit(1);\n}\n\n/* Coer\347\343o: int/float interoperam; string n\343o. */\nstatic LAMO_UNUSED long long lamo_a"
    "s_int(LamoValue value) {\n    if (value.type == LAMO_VALUE_STRING) {\n        lamo_runtime_type_error(\"expected int-com"
    "patible value, got string\");\n    }\n    if (value.type == LAMO_VALUE_FLOAT) {\n        return (long long)value.float_v"
    "alue;\n    }\n    return value.int_value;\n}\n\nstatic LAMO_UNUSED double lamo_as_float(LamoValue value) {\n    if (valu"
    "e.type == LAMO_VALUE_STRING) {\n        lamo_runtime_type_error(\"expected number, got string\");\n    }\n    if (value."
    "type == LAMO_VALUE_FLOAT) {\n        return value.float_value;\n    }\n    return (double)value.int_value;\n}\n\nstatic "
    "LAMO_UNUSED const char* lamo_as_cstring(LamoValue value) {\n    if (value.type != LAMO_VALUE_STRING) {\n        lamo_run"
    "time_type_error(\"expected string value\");\n    }\n    return value.string_value ? value.string_value : \"\";\n}\n\nsta"
    "tic LAMO_UNUSED char* lamo_value_to_owned_string(LamoValue value) {\n    char buffer[64];\n    if (value.type == LAMO_VA"
    "LUE_STRING) {\n        return lamo_heap_strdup(lamo_as_cstring(value));\n    }\n    if (value.type == LAMO_VALUE_FLOAT) "
    "{\n        snprintf(buffer, sizeof(buffer), \"%g\", value.float_value);\n        return lamo_heap_strdup(buffer);\n    }"
    "\n    if (value.type == LAMO_VALUE_BOOL) {\n        return lamo_heap_strdup(value.int_value ? \"true\" : \"false\");\n  "
    "  }\n    if (value.type == LAMO_VALUE_ARRAY || value.type == LAMO_VALUE_STRUCT) {\n        /* Recursively render the arr"
    "ay/struct into a heap string. We\n         * build a temporary buffer, then strdup it. The capacity grows\n         * as"
    " needed. */\n        size_t cap = 64;\n        size_t len = 0;\n        char* out = (char*)malloc(cap);\n        if (!ou"
    "t) return lamo_heap_strdup(\"\");\n        out[0] = '\\0';\n        #define LAMO_APPEND_STR(s) do { \\\n            size"
    "_t addlen = strlen(s); \\\n            if (len + addlen + 1 > cap) { \\\n                while (len + addlen + 1 > cap) "
    "cap *= 2; \\\n                char* grown = (char*)realloc(out, cap); \\\n                if (!grown) { free(out); retur"
    "n lamo_heap_strdup(\"\"); } \\\n                out = grown; \\\n            } \\\n            memcpy(out + len, s, addl"
    "en); \\\n            len += addlen; \\\n            out[len] = '\\0'; \\\n        } while (0)\n        LAMO_APPEND_STR(v"
    "alue.type == LAMO_VALUE_STRUCT ? \"{ \" : \"[\");\n        if (value.array_value) {\n            long long i;\n         "
    "   for (i = 0; i < value.array_value->count; i++) {\n                if (i > 0) LAMO_APPEND_STR(\", \");\n              "
    "  char* elem = lamo_value_to_owned_string(value.array_value->items[i]);\n                LAMO_APPEND_STR(elem);\n       "
    "         free(elem);\n            }\n        }\n        LAMO_APPEND_STR(value.type == LAMO_VALUE_STRUCT ? \" }\" : \"]\""
    ");\n        #undef LAMO_APPEND_STR\n        return out;\n    }\n    snprintf(buffer, sizeof(buffer), \"%lld\", value.int"
    "_value);\n    return lamo_heap_strdup(buffer);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_concat_values(LamoValue left, Lam"
    "oValue right) {\n    char* left_text = lamo_value_to_owned_string(left);\n    char* right_text = lamo_value_to_owned_str"
    "ing(right);\n    size_t left_length = strlen(left_text);\n    size_t right_length = strlen(right_text);\n    char* combi"
    "ned = (char*)lamo_arena_alloc(left_length + right_length + 1);\n    memcpy(combined, left_text, left_length);\n    memcp"
    "y(combined + left_length, right_text, right_length + 1);\n    free(left_text);\n    free(right_text);\n    return lamo_m"
    "ake_string(combined);\n}\n\n/*\n * Truthiness (DOCUMENTED \20024 see README \"Truthiness\" section):\n *\n *   - int:   "
    "  non-zero is true\n *   - float:   non-zero is true (0.0 is false)\n *   - bool:    the value itself\n *   - string:  n"
    "on-empty is true (\"\" is false)\n *\n * This is Python-like truthiness, NOT Rust-like strict bool. `if (5)` and\n * `if"
    " (\"abc\")` are both valid and true. `if (0)` and `if (\"\")` are false.\n */\nstatic LAMO_UNUSED int lamo_is_truthy(Lam"
    "oValue value) {\n    if (value.type == LAMO_VALUE_STRING) {\n        return value.string_value && value.string_value[0] "
    "!= '\\0';\n    }\n    if (value.type == LAMO_VALUE_FLOAT) {\n        return value.float_value != 0.0;\n    }\n    if (va"
    "lue.type == LAMO_VALUE_ARRAY) {\n        /* Sprint 3: arrays are truthy if non-empty. */\n        return value.array_val"
    "ue != NULL && value.array_value->count > 0;\n    }\n    if (value.type == LAMO_VALUE_STRUCT) {\n        /* Phase 2: a st"
    "ruct value is always truthy (even an empty one\n         * represents a constructed object). */\n        return value.ar"
    "ray_value != NULL;\n    }\n    return value.int_value != 0;\n}\n\nstatic LAMO_UNUSED void lamo_print_value(LamoValue val"
    "ue) {\n    if (value.type == LAMO_VALUE_STRING) {\n        printf(\"%s\\n\", value.string_value ? value.string_value : "
    "\"\");\n    } else if (value.type == LAMO_VALUE_FLOAT) {\n        printf(\"%g\\n\", value.float_value);\n    } else if ("
    "value.type == LAMO_VALUE_ARRAY) {\n        /* Sprint 3: print arrays in Python-like [a, b, c] form. Each\n         * ele"
    "ment is printed via its own lamo_value_to_owned_string\n         * representation so strings show without quotes (matchi"
    "ng\n         * the existing `print` semantics for top-level values). */\n        long long i;\n        printf(\"[\");\n "
    "       if (value.array_value) {\n            for (i = 0; i < value.array_value->count; i++) {\n                if (i > 0"
    ") printf(\", \");\n                /* Reuse lamo_value_to_owned_string to format each element. */\n                char*"
    " text = lamo_value_to_owned_string(value.array_value->items[i]);\n                printf(\"%s\", text);\n               "
    " free(text);\n            }\n        }\n        printf(\"]\\n\");\n    } else if (value.type == LAMO_VALUE_STRUCT) {\n  "
    "      /* Phase 2: print structs in `S { v0, v1, ... }` form (nameless;\n         * see lamo_print_struct_named for the s"
    "emantic-pass variant that\n         * DOES know the name). */\n        long long i;\n        printf(\"{ \");\n        if"
    " (value.array_value) {\n            for (i = 0; i < value.array_value->count; i++) {\n                if (i > 0) printf("
    "\", \");\n                char* text = lamo_value_to_owned_string(value.array_value->items[i]);\n                printf("
    "\"%s\", text);\n                free(text);\n            }\n        }\n        printf(\" }\\n\");\n    } else if (value."
    "type == LAMO_VALUE_BOOL) {\n        /* SPEC \2478 (2.6.0 decision): booleans render as `true`/`false`,\n         * match"
    "ing the bool literals in the language, the array/struct\n         * element renderer (lamo_value_to_owned_string), and t"
    "he eval\n         * interpreter. The legacy `1`/`0` form was an inconsistency:\n         * the SAME value printed `true`"
    " inside an array and `1` at the\n         * top level. Numeric coercion is available explicitly via\n         * `int(b)`"
    "; truthiness rules (\2476.3) are unchanged. */\n        printf(value.int_value ? \"true\\n\" : \"false\\n\");\n    } els"
    "e {\n        printf(\"%lld\\n\", value.int_value);\n    }\n}\n\n/* Backend-alignment: print() with semantic type informa"
    "tion. When the\n * compiler statically knows the argument's struct type, codegen calls\n * this instead of the plain pri"
    "nter so output carries the type NAME \20024\n * `Player { 10, x }` instead of `{ 10, x }`. The name travels as a C\n * s"
    "tring literal from the semantic pass (sema_struct_name); NULL falls\n * back to the exact legacy layout so existing beha"
    "vior is preserved\n * bit-for-bit for untyped call sites. */\nstatic LAMO_UNUSED void lamo_print_struct_named(LamoValue "
    "value, const char* struct_name) {\n    if (value.type != LAMO_VALUE_STRUCT || !struct_name || !struct_name[0]) {\n      "
    "  lamo_print_value(value);\n        return;\n    }\n    long long i;\n    printf(\"%s { \", struct_name);\n    if (value"
    ".array_value) {\n        for (i = 0; i < value.array_value->count; i++) {\n            if (i > 0) printf(\", \");\n     "
    "       char* text = lamo_value_to_owned_string(value.array_value->items[i]);\n            printf(\"%s\", text);\n       "
    "     free(text);\n        }\n    }\n    printf(\" }\\n\");\n}\n\n/* ----------------------------------------------------"
    "-------------- */\n/* Sprint 3: Array runtime                                            */\n/* ------------------------"
    "------------------------------------------ */\n\n/* Allocate a new LamoArray with the given initial capacity. The struct"
    "\n * is registered in the arena (via lamo_arena_alloc_traced, so the GC\n * knows to trace its items[]); its `items` buf"
    "fer is also arena-tracked\n * (via plain lamo_arena_alloc \20024 it's reached during trace via the parent\n * LamoArray'"
    "s mark phase, so it doesn't need its own is_array flag). */\nstatic LAMO_UNUSED LamoArray* lamo_array_alloc(long long in"
    "itial_capacity) {\n    LamoArray* array;\n    if (initial_capacity < 4) initial_capacity = 4;\n    array = (LamoArray*)l"
    "amo_arena_alloc_traced(sizeof(LamoArray));\n    array->items = (LamoValue*)lamo_arena_alloc(sizeof(LamoValue) * (size_t)"
    "initial_capacity);\n    array->count = 0;\n    array->capacity = initial_capacity;\n    return array;\n}\n\n/* Build an "
    "array value from a list of LamoValues. Used by the codegen\n * for array literals. The values are copied into a new buff"
    "er. */\nstatic LAMO_UNUSED LamoValue lamo_array_from_values(LamoValue* values, long long count) {\n    long long i;\n   "
    " LamoArray* array = lamo_array_alloc(count > 0 ? count : 4);\n    for (i = 0; i < count; i++) {\n        array->items[i]"
    " = values[i];\n    }\n    array->count = count;\n    return lamo_make_array(array);\n}\n\n/* Get element at index. Negat"
    "ive indices count from the end (-1 is the\n * last element). Out-of-range indices abort with a clear runtime error. */\n"
    "static LAMO_UNUSED LamoValue lamo_array_get(LamoValue array_val, long long index) {\n    LamoArray* array;\n    if (arra"
    "y_val.type != LAMO_VALUE_ARRAY) {\n        lamo_runtime_type_error(\"expected array value for indexing\");\n    }\n    a"
    "rray = array_val.array_value;\n    if (!array) {\n        lamo_runtime_type_error(\"indexed array is NULL (internal erro"
    "r)\");\n    }\n    if (index < 0) index += array->count;  /* Python-like negative index */\n    if (index < 0 || index >"
    "= array->count) {\n        fprintf(stderr, \"runtime error: array index %lld out of bounds (array length %lld)\\n\",\n  "
    "              index, array->count);\n        exit(1);\n    }\n    return array->items[index];\n}\n\n/* Returns the lengt"
    "h of an array. */\nstatic LAMO_UNUSED LamoValue lamo_array_len(LamoValue array_val) {\n    if (array_val.type != LAMO_VA"
    "LUE_ARRAY) {\n        lamo_runtime_type_error(\"expected array value for len()\");\n    }\n    if (!array_val.array_valu"
    "e) {\n        return lamo_make_int(0);\n    }\n    return lamo_make_int(array_val.array_value->count);\n}\n\n/* Push a v"
    "alue onto the end of the array. Grows the internal buffer if\n * needed via realloc (the new buffer is registered in the"
    " arena; the old\n * one is leaked until program exit, same as the existing string-arena\n * policy). Returns 0 on succes"
    "s. */\nstatic LAMO_UNUSED LamoValue lamo_array_push(LamoValue array_val, LamoValue value) {\n    LamoArray* array;\n    "
    "if (array_val.type != LAMO_VALUE_ARRAY) {\n        lamo_runtime_type_error(\"expected array value for push()\");\n    }"
    "\n    array = array_val.array_value;\n    if (!array) {\n        lamo_runtime_type_error(\"push() target array is NULL ("
    "internal error)\");\n    }\n    if (array->count == array->capacity) {\n        long long new_cap = array->capacity * 2;"
    "\n        LamoValue* new_items = (LamoValue*)lamo_arena_alloc(sizeof(LamoValue) * (size_t)new_cap);\n        memcpy(new_"
    "items, array->items, sizeof(LamoValue) * (size_t)array->count);\n        array->items = new_items;\n        array->capac"
    "ity = new_cap;\n    }\n    array->items[array->count++] = value;\n    return lamo_make_int(0);\n}\n\n/* Pop the last ele"
    "ment from the array and return it. Aborts if the\n * array is empty. */\nstatic LAMO_UNUSED LamoValue lamo_array_pop(Lam"
    "oValue array_val) {\n    LamoArray* array;\n    LamoValue popped;\n    if (array_val.type != LAMO_VALUE_ARRAY) {\n      "
    "  lamo_runtime_type_error(\"expected array value for pop()\");\n    }\n    array = array_val.array_value;\n    if (!arra"
    "y || array->count == 0) {\n        fprintf(stderr, \"runtime error: pop() on empty array\\n\");\n        exit(1);\n    }"
    "\n    popped = array->items[--array->count];\n    return popped;\n}\n\n/* Phase 2: array index assignment \20024 `arr[i]"
    " = value`.\n * Returns the assigned value (so it can be used in expressions, e.g.\n * `let old = (arr[0] = 42);` though "
    "that's not idiomatic). */\nstatic LAMO_UNUSED LamoValue lamo_array_set(LamoValue array_val, long long index, LamoValue v"
    "alue) {\n    LamoArray* array;\n    if (array_val.type != LAMO_VALUE_ARRAY) {\n        lamo_runtime_type_error(\"expecte"
    "d array value for index assignment\");\n    }\n    array = array_val.array_value;\n    if (!array) {\n        lamo_runti"
    "me_type_error(\"index-assign target array is NULL (internal error)\");\n    }\n    if (index < 0) index += array->count;"
    "\n    if (index < 0 || index >= array->count) {\n        fprintf(stderr, \"runtime error: array index %lld out of bounds"
    " in assignment (array length %lld)\\n\",\n                index, array->count);\n        exit(1);\n    }\n    array->ite"
    "ms[index] = value;\n    return value;\n}\n\n/* Phase 2: struct helpers. Structs reuse LamoArray under the hood.\n *\n * "
    "lamo_struct_alloc(n) - create a struct value with n zero-initialized\n *   fields (each slot is lamo_make_int(0)).\n * l"
    "amo_struct_get(sval, i) - read field at index i (lamo_array_get works\n *   too, but this function emits a clearer error"
    " message).\n * lamo_struct_set(sval, i, value) - write field at index i. Returns the\n *   assigned value (so it can be "
    "used in expressions). */\nstatic LAMO_UNUSED LamoValue lamo_struct_alloc(long long field_count) {\n    long long i;\n   "
    " LamoArray* array;\n    if (field_count < 0) field_count = 0;\n    array = lamo_array_alloc(field_count > 0 ? field_coun"
    "t : 4);\n    for (i = 0; i < field_count; i++) {\n        array->items[i] = lamo_make_int(0);\n    }\n    array->count ="
    " field_count;\n    return lamo_make_struct(array);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_struct_get(LamoValue struct_v"
    "al, long long index) {\n    LamoArray* array;\n    if (struct_val.type != LAMO_VALUE_STRUCT) {\n        lamo_runtime_typ"
    "e_error(\"expected struct value for field access\");\n    }\n    array = struct_val.array_value;\n    if (!array) {\n   "
    "     lamo_runtime_type_error(\"field access on NULL struct (internal error)\");\n    }\n    if (index < 0 || index >= ar"
    "ray->count) {\n        fprintf(stderr, \"runtime error: struct field index %lld out of bounds (field count %lld)\\n\",\n"
    "                index, array->count);\n        exit(1);\n    }\n    return array->items[index];\n}\n\nstatic LAMO_UNUSED"
    " LamoValue lamo_struct_set(LamoValue struct_val, long long index, LamoValue value) {\n    LamoArray* array;\n    if (str"
    "uct_val.type != LAMO_VALUE_STRUCT) {\n        lamo_runtime_type_error(\"expected struct value for field assignment\");\n"
    "    }\n    array = struct_val.array_value;\n    if (!array) {\n        lamo_runtime_type_error(\"field-assign target str"
    "uct is NULL (internal error)\");\n    }\n    if (index < 0 || index >= array->count) {\n        fprintf(stderr, \"runtim"
    "e error: struct field index %lld out of bounds in assignment (field count %lld)\\n\",\n                index, array->cou"
    "nt);\n        exit(1);\n    }\n    array->items[index] = value;\n    return value;\n}\n\n/* Opera\347\365es aritm\351tic"
    "as: se qualquer operando for float, resultado \351 float. */\nstatic LAMO_UNUSED LamoValue lamo_add(LamoValue left, Lamo"
    "Value right) {\n    if (left.type == LAMO_VALUE_STRING || right.type == LAMO_VALUE_STRING) {\n        return lamo_concat"
    "_values(left, right);\n    }\n    if (left.type == LAMO_VALUE_FLOAT || right.type == LAMO_VALUE_FLOAT) {\n        return"
    " lamo_make_float(lamo_as_float(left) + lamo_as_float(right));\n    }\n    return lamo_make_int(lamo_as_int(left) + lamo_"
    "as_int(right));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_sub(LamoValue left, LamoValue right) {\n    if (left.type == LAM"
    "O_VALUE_FLOAT || right.type == LAMO_VALUE_FLOAT) {\n        return lamo_make_float(lamo_as_float(left) - lamo_as_float(r"
    "ight));\n    }\n    return lamo_make_int(lamo_as_int(left) - lamo_as_int(right));\n}\n\nstatic LAMO_UNUSED LamoValue lam"
    "o_mul(LamoValue left, LamoValue right) {\n    if (left.type == LAMO_VALUE_FLOAT || right.type == LAMO_VALUE_FLOAT) {\n  "
    "      return lamo_make_float(lamo_as_float(left) * lamo_as_float(right));\n    }\n    return lamo_make_int(lamo_as_int(l"
    "eft) * lamo_as_int(right));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_div(LamoValue left, LamoValue right) {\n    if (left"
    ".type == LAMO_VALUE_FLOAT || right.type == LAMO_VALUE_FLOAT) {\n        double lhs = lamo_as_float(left);\n        doubl"
    "e rhs = lamo_as_float(right);\n        if (rhs == 0.0) {\n            fprintf(stderr, \"runtime error: division by zero"
    "\\n\");\n            exit(1);\n        }\n        return lamo_make_float(lhs / rhs);\n    }\n    {\n        long long lh"
    "s = lamo_as_int(left);\n        long long rhs = lamo_as_int(right);\n        if (rhs == 0) {\n            fprintf(stderr"
    ", \"runtime error: division by zero\\n\");\n            exit(1);\n        }\n        return lamo_make_int(lhs / rhs);\n "
    "   }\n}\n\nstatic LAMO_UNUSED LamoValue lamo_mod(LamoValue left, LamoValue right) {\n    if (left.type == LAMO_VALUE_FLO"
    "AT || right.type == LAMO_VALUE_FLOAT) {\n        double lhs = lamo_as_float(left);\n        double rhs = lamo_as_float(r"
    "ight);\n        if (rhs == 0.0) {\n            fprintf(stderr, \"runtime error: modulo by zero\\n\");\n            exit("
    "1);\n        }\n        return lamo_make_float(fmod(lhs, rhs));\n    }\n    {\n        long long lhs = lamo_as_int(left)"
    ";\n        long long rhs = lamo_as_int(right);\n        if (rhs == 0) {\n            fprintf(stderr, \"runtime error: mo"
    "dulo by zero\\n\");\n            exit(1);\n        }\n        return lamo_make_int(lhs % rhs);\n    }\n}\n\nstatic LAMO_"
    "UNUSED LamoValue lamo_negate(LamoValue value) {\n    if (value.type == LAMO_VALUE_FLOAT) return lamo_make_float(-value.f"
    "loat_value);\n    return lamo_make_int(-lamo_as_int(value));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_not(LamoValue value"
    ") {\n    return lamo_make_bool(!lamo_is_truthy(value));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_less(LamoValue left, Lam"
    "oValue right) {\n    if (left.type == LAMO_VALUE_FLOAT || right.type == LAMO_VALUE_FLOAT) {\n        return lamo_make_bo"
    "ol(lamo_as_float(left) < lamo_as_float(right));\n    }\n    return lamo_make_bool(lamo_as_int(left) < lamo_as_int(right)"
    ");\n}\n\nstatic LAMO_UNUSED LamoValue lamo_greater(LamoValue left, LamoValue right) {\n    if (left.type == LAMO_VALUE_F"
    "LOAT || right.type == LAMO_VALUE_FLOAT) {\n        return lamo_make_bool(lamo_as_float(left) > lamo_as_float(right));\n "
    "   }\n    return lamo_make_bool(lamo_as_int(left) > lamo_as_int(right));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_less_eq"
    "ual(LamoValue left, LamoValue right) {\n    if (left.type == LAMO_VALUE_FLOAT || right.type == LAMO_VALUE_FLOAT) {\n    "
    "    return lamo_make_bool(lamo_as_float(left) <= lamo_as_float(right));\n    }\n    return lamo_make_bool(lamo_as_int(le"
    "ft) <= lamo_as_int(right));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_greater_equal(LamoValue left, LamoValue right) {\n  "
    "  if (left.type == LAMO_VALUE_FLOAT || right.type == LAMO_VALUE_FLOAT) {\n        return lamo_make_bool(lamo_as_float(le"
    "ft) >= lamo_as_float(right));\n    }\n    return lamo_make_bool(lamo_as_int(left) >= lamo_as_int(right));\n}\n\nstatic L"
    "AMO_UNUSED LamoValue lamo_equal(LamoValue left, LamoValue right) {\n    /* Phase 2: arrays and structs compare by identi"
    "ty (same pointer).\n     * Deep equality would be expensive and surprising for mutable values. */\n    if (left.type == "
    "LAMO_VALUE_ARRAY || left.type == LAMO_VALUE_STRUCT ||\n        right.type == LAMO_VALUE_ARRAY || right.type == LAMO_VALU"
    "E_STRUCT) {\n        if (left.type != right.type) return lamo_make_bool(0);\n        return lamo_make_bool(left.array_va"
    "lue == right.array_value);\n    }\n    if (left.type == LAMO_VALUE_STRING || right.type == LAMO_VALUE_STRING) {\n       "
    " if (left.type != LAMO_VALUE_STRING || right.type != LAMO_VALUE_STRING) {\n            return lamo_make_bool(0);\n      "
    "  }\n        return lamo_make_bool(strcmp(lamo_as_cstring(left), lamo_as_cstring(right)) == 0);\n    }\n    if (left.typ"
    "e == LAMO_VALUE_FLOAT || right.type == LAMO_VALUE_FLOAT) {\n        return lamo_make_bool(lamo_as_float(left) == lamo_as"
    "_float(right));\n    }\n    return lamo_make_bool(lamo_as_int(left) == lamo_as_int(right));\n}\n\nstatic LAMO_UNUSED Lam"
    "oValue lamo_not_equal(LamoValue left, LamoValue right) {\n    LamoValue result = lamo_equal(left, right);\n    return la"
    "mo_make_bool(!lamo_is_truthy(result));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_and(LamoValue left, LamoValue right) {\n "
    "   return lamo_make_bool(lamo_is_truthy(left) && lamo_is_truthy(right));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_or(Lamo"
    "Value left, LamoValue right) {\n    return lamo_make_bool(lamo_is_truthy(left) || lamo_is_truthy(right));\n}\n\nstatic L"
    "AMO_UNUSED void lamo_print_prompt(LamoValue prompt) {\n    if (prompt.type == LAMO_VALUE_STRING) {\n        printf(\"%s"
    "\", lamo_as_cstring(prompt));\n    } else if (prompt.type == LAMO_VALUE_FLOAT) {\n        printf(\"%g\", prompt.float_va"
    "lue);\n    } else {\n        printf(\"%lld\", prompt.int_value);\n    }\n    fflush(stdout);\n}\n\n/* input(prompt) - re"
    "ads an integer from stdin. Kept for compatibility with\n * existing programs. To read strings, use input_str(prompt). */"
    "\nstatic LAMO_UNUSED LamoValue lamo_input_value(LamoValue prompt) {\n    long long value;\n    lamo_print_prompt(prompt)"
    ";\n    if (scanf(\"%lld\", &value) != 1) {\n        value = 0;\n    }\n    return lamo_make_int(value);\n}\n\n/* input_i"
    "nt(prompt) - explicit alias for input(), reads long long. */\nstatic LAMO_UNUSED LamoValue lamo_input_int_value(LamoValu"
    "e prompt) {\n    long long value;\n    lamo_print_prompt(prompt);\n    if (scanf(\"%lld\", &value) != 1) {\n        valu"
    "e = 0;\n    }\n    return lamo_make_int(value);\n}\n\n/* input_str(prompt) - reads one line of text from stdin, without "
    "the trailing\n * newline. The returned string is allocated in the string arena (freed at\n * atexit). */\nstatic LAMO_UN"
    "USED LamoValue lamo_input_str_value(LamoValue prompt) {\n    char buffer[1024];\n    size_t length;\n    char* owned;\n "
    "   lamo_print_prompt(prompt);\n    if (fgets(buffer, sizeof(buffer), stdin) == NULL) {\n        buffer[0] = '\\0';\n    "
    "}\n    length = strlen(buffer);\n    while (length > 0 && (buffer[length-1] == '\\n' || buffer[length-1] == '\\r')) {\n "
    "       buffer[--length] = '\\0';\n    }\n    owned = lamo_arena_strdup(buffer);\n    return lamo_make_string(owned);\n}"
    "\n\nstatic LAMO_UNUSED LamoValue lamo_isnumber_value(LamoValue value) {\n    return lamo_make_bool(value.type == LAMO_VA"
    "LUE_INT || value.type == LAMO_VALUE_FLOAT);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_isstring_value(LamoValue value) {\n "
    "   return lamo_make_bool(value.type == LAMO_VALUE_STRING);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_isarray_value(LamoVal"
    "ue value) {\n    return lamo_make_bool(value.type == LAMO_VALUE_ARRAY);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_abs_valu"
    "e(LamoValue value) {\n    if (value.type == LAMO_VALUE_FLOAT) {\n        double v = value.float_value;\n        return l"
    "amo_make_float(v < 0 ? -v : v);\n    }\n    {\n        long long raw = lamo_as_int(value);\n        return lamo_make_int"
    "(raw < 0 ? -raw : raw);\n    }\n}\n\n/* ------------------------------------------------------------------ */\n/* 2. GUI"
    " runtime (only emitted when the program uses gui_* builtins) */\n/* ----------------------------------------------------"
    "-------------- */\n#ifdef LAMO_NEEDS_GUI_RUNTIME\n\n/* GC Step 4: periodic collection hook for the GUI event loop. The u"
    "ser's\n * render loop calls gui_should_close() once per frame; we use that as the\n * heartbeat and run gc_collect() eve"
    "ry LAMO_GUI_GC_INTERVAL frames. The\n * interval is conservative (1000) so frame cost stays predictable. */\n#define LAM"
    "O_GUI_GC_INTERVAL 1000\nstatic long long lamo_gui_frame_counter = 0;\n\n#ifdef _WIN32\n#include <windows.h>\n\ntypedef s"
    "truct {\n    HWND hwnd;\n    HDC back_dc;\n    HBITMAP back_bitmap;\n    HFONT font;\n    int width;\n    int height;\n "
    "   int is_open;\n} LamoGuiState;\n\nstatic LamoGuiState lamo_gui = {0};\nstatic int lamo_gui_registered = 0;\n\nstatic L"
    "RESULT CALLBACK lamo_gui_window_proc(HWND hwnd, UINT message, WPARAM w_param, LPARAM l_param) {\n    (void)l_param;\n   "
    " switch (message) {\n        case WM_CLOSE:\n            lamo_gui.is_open = 0;\n            DestroyWindow(hwnd);\n      "
    "      return 0;\n        case WM_DESTROY:\n            lamo_gui.is_open = 0;\n            PostQuitMessage(0);\n         "
    "   return 0;\n        default:\n            return DefWindowProcA(hwnd, message, w_param, l_param);\n    }\n}\n\nstatic "
    "void lamo_gui_process_messages(void) {\n    MSG message;\n    while (PeekMessageA(&message, NULL, 0, 0, PM_REMOVE)) {\n "
    "       if (message.message == WM_QUIT) {\n            lamo_gui.is_open = 0;\n        }\n        TranslateMessage(&messag"
    "e);\n        DispatchMessageA(&message);\n    }\n}\n\nstatic int lamo_gui_open(int width, int height, const char* title)"
    " {\n    WNDCLASSA window_class;\n    HDC window_dc;\n    RECT rect;\n    if (lamo_gui.is_open) { return 1; }\n    if (!l"
    "amo_gui_registered) {\n        ZeroMemory(&window_class, sizeof(window_class));\n        window_class.lpfnWndProc = lamo"
    "_gui_window_proc;\n        window_class.hInstance = GetModuleHandleA(NULL);\n        window_class.lpszClassName = \"Lamo"
    "GuiWindow\";\n        window_class.hCursor = LoadCursor(NULL, IDC_ARROW);\n        window_class.hbrBackground = (HBRUSH)"
    "(COLOR_WINDOW + 1);\n        if (!RegisterClassA(&window_class)) {\n            DWORD error_code = GetLastError();\n    "
    "        if (error_code != ERROR_CLASS_ALREADY_EXISTS) {\n                fprintf(stderr, \"failed to register GUI window"
    " class (error %lu)\\n\", (unsigned long)error_code);\n                return 0;\n            }\n        }\n        lamo_"
    "gui_registered = 1;\n    }\n    lamo_gui.width = width;\n    lamo_gui.height = height;\n    rect.left = 0; rect.top = 0;"
    " rect.right = width; rect.bottom = height;\n    AdjustWindowRect(&rect, WS_OVERLAPPEDWINDOW, 0);\n    lamo_gui.hwnd = Cr"
    "eateWindowExA(0, \"LamoGuiWindow\", title, WS_OVERLAPPEDWINDOW,\n        CW_USEDEFAULT, CW_USEDEFAULT, rect.right - rect"
    ".left, rect.bottom - rect.top,\n        NULL, NULL, GetModuleHandleA(NULL), NULL);\n    if (!lamo_gui.hwnd) { fprintf(st"
    "derr, \"failed to create GUI window\\n\"); return 0; }\n    ShowWindow(lamo_gui.hwnd, SW_SHOW);\n    UpdateWindow(lamo_g"
    "ui.hwnd);\n    window_dc = GetDC(lamo_gui.hwnd);\n    lamo_gui.back_dc = CreateCompatibleDC(window_dc);\n    lamo_gui.ba"
    "ck_bitmap = CreateCompatibleBitmap(window_dc, width, height);\n    SelectObject(lamo_gui.back_dc, lamo_gui.back_bitmap);"
    "\n    ReleaseDC(lamo_gui.hwnd, window_dc);\n    SetBkMode(lamo_gui.back_dc, TRANSPARENT);\n    lamo_gui.font = CreateFon"
    "tA(24, 0, 0, 0, FW_NORMAL, 0, 0, 0, ANSI_CHARSET,\n        OUT_DEFAULT_PRECIS, CLIP_DEFAULT_PRECIS, CLEARTYPE_QUALITY,\n"
    "        DEFAULT_PITCH | FF_DONTCARE, \"Segoe UI\");\n    if (lamo_gui.font) { SelectObject(lamo_gui.back_dc, lamo_gui.fo"
    "nt); }\n    lamo_gui.is_open = 1;\n    return 1;\n}\n\nstatic int lamo_gui_should_close(void) {\n    lamo_gui_process_me"
    "ssages();\n    /* GC Step 4: periodically reclaim unreachable allocations so the GUI\n     * event loop doesn't grow the"
    " arena without bound. The interval is\n     * conservative (every 1000 frames) so frame cost stays predictable. */\n    "
    "if (++lamo_gui_frame_counter >= LAMO_GUI_GC_INTERVAL) {\n        lamo_gui_frame_counter = 0;\n        lamo_gc_collect();"
    "\n    }\n    return lamo_gui.is_open ? 0 : 1;\n}\n\nstatic void lamo_gui_begin_frame(int r, int g, int b) {\n    RECT re"
    "ct; HBRUSH brush;\n    if (!lamo_gui.is_open) return;\n    rect.left = 0; rect.top = 0; rect.right = lamo_gui.width; rec"
    "t.bottom = lamo_gui.height;\n    brush = CreateSolidBrush(RGB(r, g, b));\n    FillRect(lamo_gui.back_dc, &rect, brush);"
    "\n    DeleteObject(brush);\n}\n\nstatic void lamo_gui_draw_rect(int x, int y, int width, int height, int r, int g, int b"
    ") {\n    RECT rect; HBRUSH brush;\n    if (!lamo_gui.is_open) return;\n    rect.left = x; rect.top = y; rect.right = x +"
    " width; rect.bottom = y + height;\n    brush = CreateSolidBrush(RGB(r, g, b));\n    FillRect(lamo_gui.back_dc, &rect, br"
    "ush);\n    DeleteObject(brush);\n}\n\nstatic void lamo_gui_draw_text(const char* text, int x, int y, int r, int g, int b"
    ") {\n    if (!lamo_gui.is_open) return;\n    SetTextColor(lamo_gui.back_dc, RGB(r, g, b));\n    TextOutA(lamo_gui.back_d"
    "c, x, y, text, (int)strlen(text));\n}\n\nstatic void lamo_gui_end_frame(void) {\n    HDC window_dc;\n    if (!lamo_gui.i"
    "s_open) return;\n    window_dc = GetDC(lamo_gui.hwnd);\n    BitBlt(window_dc, 0, 0, lamo_gui.width, lamo_gui.height, lam"
    "o_gui.back_dc, 0, 0, SRCCOPY);\n    ReleaseDC(lamo_gui.hwnd, window_dc);\n}\n\nstatic void lamo_gui_close(void) {\n    i"
    "f (lamo_gui.font) { DeleteObject(lamo_gui.font); lamo_gui.font = NULL; }\n    if (lamo_gui.back_bitmap) { DeleteObject(l"
    "amo_gui.back_bitmap); lamo_gui.back_bitmap = NULL; }\n    if (lamo_gui.back_dc) { DeleteDC(lamo_gui.back_dc); lamo_gui.b"
    "ack_dc = NULL; }\n    if (lamo_gui.hwnd) { DestroyWindow(lamo_gui.hwnd); lamo_gui.hwnd = NULL; }\n    lamo_gui.is_open ="
    " 0;\n}\n#elif defined(__unix__) || defined(__unix) || defined(__linux__) || defined(__APPLE__)\n/* Backend X11 for Linux"
    "/Mac. C and Xlib are trivial to link (-lX11). */\n#include <X11/Xlib.h>\n#include <X11/Xutil.h>\n#include <X11/Xos.h>\n"
    "\ntypedef struct {\n    Display* display;\n    Window window;\n    GC gc;\n    Pixmap back_pixmap;\n    XFontStruct* fon"
    "t;\n    int width;\n    int height;\n    int is_open;\n    Atom wm_delete;\n} LamoGuiState;\n\nstatic LamoGuiState lamo_"
    "gui = {0};\n\nstatic int lamo_gui_open(int width, int height, const char* title) {\n    int screen;\n    if (lamo_gui.is"
    "_open) { return 1; }\n    lamo_gui.display = XOpenDisplay(NULL);\n    if (!lamo_gui.display) {\n        fprintf(stderr, "
    "\"failed to open X11 display\\n\");\n        return 0;\n    }\n    screen = DefaultScreen(lamo_gui.display);\n    lamo_g"
    "ui.width = width;\n    lamo_gui.height = height;\n    lamo_gui.window = XCreateSimpleWindow(lamo_gui.display,\n        R"
    "ootWindow(lamo_gui.display, screen),\n        0, 0, width, height, 1,\n        BlackPixel(lamo_gui.display, screen),\n  "
    "      WhitePixel(lamo_gui.display, screen));\n    XSelectInput(lamo_gui.display, lamo_gui.window,\n        ExposureMask "
    "| StructureNotifyMask);\n    XStoreName(lamo_gui.display, lamo_gui.window, title ? title : \"Lamo\");\n    lamo_gui.wm_d"
    "elete = XInternAtom(lamo_gui.display, \"WM_DELETE_WINDOW\", False);\n    XSetWMProtocols(lamo_gui.display, lamo_gui.wind"
    "ow, &lamo_gui.wm_delete, 1);\n    lamo_gui.gc = XCreateGC(lamo_gui.display, lamo_gui.window, 0, NULL);\n    lamo_gui.bac"
    "k_pixmap = XCreatePixmap(lamo_gui.display, lamo_gui.window,\n        width, height, DefaultDepth(lamo_gui.display, scree"
    "n));\n    lamo_gui.font = XLoadQueryFont(lamo_gui.display, \"fixed\");\n    if (lamo_gui.font) { XSetFont(lamo_gui.displ"
    "ay, lamo_gui.gc, lamo_gui.font->fid); }\n    XMapWindow(lamo_gui.display, lamo_gui.window);\n    XFlush(lamo_gui.display"
    ");\n    lamo_gui.is_open = 1;\n    return 1;\n}\n\nstatic void lamo_gui_process_messages(void) {\n    XEvent event;\n   "
    " while (XPending(lamo_gui.display) > 0) {\n        XNextEvent(lamo_gui.display, &event);\n        if (event.type == Clie"
    "ntMessage) {\n            if ((Atom)event.xclient.data.l[0] == lamo_gui.wm_delete) {\n                lamo_gui.is_open ="
    " 0;\n            }\n        }\n    }\n}\n\nstatic int lamo_gui_should_close(void) {\n    if (!lamo_gui.display) return 1"
    ";\n    lamo_gui_process_messages();\n    /* GC Step 4: periodic collection (see the Windows branch above for\n     * the"
    " same pattern \20024 every 1000 frames, run gc_collect). */\n    if (++lamo_gui_frame_counter >= LAMO_GUI_GC_INTERVAL) {"
    "\n        lamo_gui_frame_counter = 0;\n        lamo_gc_collect();\n    }\n    return lamo_gui.is_open ? 0 : 1;\n}\n\nsta"
    "tic void lamo_gui_begin_frame(int r, int g, int b) {\n    XColor color;\n    if (!lamo_gui.is_open) return;\n    color.r"
    "ed = (unsigned short)(r * 257);\n    color.green = (unsigned short)(g * 257);\n    color.blue = (unsigned short)(b * 257"
    ");\n    color.flags = DoRed | DoGreen | DoBlue;\n    XAllocColor(lamo_gui.display, DefaultColormap(lamo_gui.display, 0),"
    " &color);\n    XSetForeground(lamo_gui.display, lamo_gui.gc, color.pixel);\n    XFillRectangle(lamo_gui.display, lamo_gu"
    "i.back_pixmap, lamo_gui.gc,\n        0, 0, lamo_gui.width, lamo_gui.height);\n}\n\nstatic void lamo_gui_draw_rect(int x,"
    " int y, int width, int height, int r, int g, int b) {\n    XColor color;\n    if (!lamo_gui.is_open) return;\n    color."
    "red = (unsigned short)(r * 257);\n    color.green = (unsigned short)(g * 257);\n    color.blue = (unsigned short)(b * 25"
    "7);\n    color.flags = DoRed | DoGreen | DoBlue;\n    XAllocColor(lamo_gui.display, DefaultColormap(lamo_gui.display, 0)"
    ", &color);\n    XSetForeground(lamo_gui.display, lamo_gui.gc, color.pixel);\n    XFillRectangle(lamo_gui.display, lamo_g"
    "ui.back_pixmap, lamo_gui.gc,\n        x, y, width, height);\n}\n\nstatic void lamo_gui_draw_text(const char* text, int x"
    ", int y, int r, int g, int b) {\n    XColor color;\n    if (!lamo_gui.is_open) return;\n    color.red = (unsigned short)"
    "(r * 257);\n    color.green = (unsigned short)(g * 257);\n    color.blue = (unsigned short)(b * 257);\n    color.flags ="
    " DoRed | DoGreen | DoBlue;\n    XAllocColor(lamo_gui.display, DefaultColormap(lamo_gui.display, 0), &color);\n    XSetFo"
    "reground(lamo_gui.display, lamo_gui.gc, color.pixel);\n    XDrawString(lamo_gui.display, lamo_gui.back_pixmap, lamo_gui."
    "gc,\n        x, y + (lamo_gui.font ? lamo_gui.font->ascent : 12),\n        text ? text : \"\", text ? (int)strlen(text) "
    ": 0);\n}\n\nstatic void lamo_gui_end_frame(void) {\n    if (!lamo_gui.is_open) return;\n    XCopyArea(lamo_gui.display, "
    "lamo_gui.back_pixmap, lamo_gui.window, lamo_gui.gc,\n        0, 0, lamo_gui.width, lamo_gui.height, 0, 0);\n    XFlush(l"
    "amo_gui.display);\n}\n\nstatic void lamo_gui_close(void) {\n    if (lamo_gui.font) { XFreeFont(lamo_gui.display, lamo_gu"
    "i.font); lamo_gui.font = NULL; }\n    if (lamo_gui.back_pixmap) { XFreePixmap(lamo_gui.display, lamo_gui.back_pixmap); l"
    "amo_gui.back_pixmap = 0; }\n    if (lamo_gui.gc) { XFreeGC(lamo_gui.display, lamo_gui.gc); lamo_gui.gc = NULL; }\n    if"
    " (lamo_gui.window) { XDestroyWindow(lamo_gui.display, lamo_gui.window); lamo_gui.window = 0; }\n    if (lamo_gui.display"
    ") { XCloseDisplay(lamo_gui.display); lamo_gui.display = NULL; }\n    lamo_gui.is_open = 0;\n}\n#else\nstatic int lamo_gu"
    "i_open(int width, int height, const char* title) {\n    (void)width; (void)height; (void)title;\n    fprintf(stderr, \"G"
    "UI builtins are not supported on this platform.\\n\");\n    return 0;\n}\nstatic int lamo_gui_should_close(void) { retur"
    "n 1; }\nstatic void lamo_gui_begin_frame(int r, int g, int b) { (void)r; (void)g; (void)b; }\nstatic void lamo_gui_draw_"
    "rect(int x, int y, int width, int height, int r, int g, int b) {\n    (void)x; (void)y; (void)width; (void)height; (void"
    ")r; (void)g; (void)b;\n}\nstatic void lamo_gui_draw_text(const char* text, int x, int y, int r, int g, int b) {\n    (vo"
    "id)text; (void)x; (void)y; (void)r; (void)g; (void)b;\n}\nstatic void lamo_gui_end_frame(void) {}\nstatic void lamo_gui_"
    "close(void) {}\n#endif\n\n#endif /* LAMO_NEEDS_GUI_RUNTIME */\n\n/* ----------------------------------------------------"
    "-------------- */\n/* 3. HTTP runtime (only emitted when the program uses http_* builtins) */\n/* ----------------------"
    "-------------------------------------------- */\n#ifdef LAMO_NEEDS_HTTP_RUNTIME\n\n#ifdef _WIN32\n#include <winsock2.h>"
    "\n#include <ws2tcpip.h>\ntypedef SOCKET lamo_socket_t;\n#define LAMO_INVALID_SOCKET INVALID_SOCKET\n#define lamo_close_s"
    "ocket closesocket\n#else\n#include <sys/types.h>\n#include <sys/socket.h>\n#include <netinet/in.h>\n#include <arpa/inet."
    "h>\n#include <unistd.h>\ntypedef int lamo_socket_t;\n#define LAMO_INVALID_SOCKET (-1)\n#define lamo_close_socket close\n"
    "#endif\n\ntypedef struct LamoHttpRoute {\n    char* path;\n    char* body;\n    struct LamoHttpRoute* next;\n} LamoHttpR"
    "oute;\n\nstatic LamoHttpRoute* lamo_http_routes = NULL;\nstatic int lamo_http_runtime_initialized = 0;\n\nstatic int lam"
    "o_http_init_runtime(void) {\n#ifdef _WIN32\n    WSADATA wsa_data;\n    if (!lamo_http_runtime_initialized) {\n        if"
    " (WSAStartup(MAKEWORD(2, 2), &wsa_data) != 0) {\n            fprintf(stderr, \"failed to initialize Winsock\\n\");\n    "
    "        return 0;\n        }\n        lamo_http_runtime_initialized = 1;\n    }\n#else\n    lamo_http_runtime_initialize"
    "d = 1;\n#endif\n    return 1;\n}\n\nstatic void lamo_http_add_route(const char* path, const char* body) {\n    LamoHttpR"
    "oute* route;\n    if (!lamo_http_init_runtime()) { return; }\n    route = malloc(sizeof(LamoHttpRoute));\n    if (!route"
    ") { fprintf(stderr, \"failed to allocate HTTP route\\n\"); exit(1); }\n    route->path = lamo_heap_strdup(path);\n    ro"
    "ute->body = lamo_heap_strdup(body);\n    route->next = lamo_http_routes;\n    lamo_http_routes = route;\n}\n\nstatic con"
    "st LamoHttpRoute* lamo_http_find_route(const char* path) {\n    LamoHttpRoute* current = lamo_http_routes;\n    while (c"
    "urrent) {\n        if (strcmp(current->path, path) == 0) { return current; }\n        current = current->next;\n    }\n "
    "   return NULL;\n}\n\nstatic int lamo_http_send_all(lamo_socket_t socket_fd, const char* data, int total_length) {\n    "
    "int sent = 0;\n    while (sent < total_length) {\n        int chunk = send(socket_fd, data + sent, total_length - sent, "
    "0);\n        if (chunk <= 0) { return 0; }\n        sent += chunk;\n    }\n    return 1;\n}\n\nstatic int lamo_http_send"
    "_response(lamo_socket_t client_socket, int status_code, const char* body) {\n    char header[512];\n    const char* reas"
    "on = status_code == 200 ? \"OK\" : \"Not Found\";\n    int body_length = (int)strlen(body);\n    int header_length = snp"
    "rintf(header, sizeof(header),\n        \"HTTP/1.1 %d %s\\r\\n\"\n        \"Content-Type: text/plain; charset=utf-8\\r\\n"
    "\"\n        \"Content-Length: %d\\r\\n\"\n        \"Connection: close\\r\\n\\r\\n\",\n        status_code, reason, body_"
    "length);\n    if (header_length < 0 || !lamo_http_send_all(client_socket, header, header_length)) {\n        return 0;\n"
    "    }\n    return lamo_http_send_all(client_socket, body, body_length);\n}\n\nstatic void lamo_http_handle_client(lamo_s"
    "ocket_t client_socket) {\n    char request[2048];\n    char method[16];\n    char path[1024];\n    int received = recv(c"
    "lient_socket, request, (int)sizeof(request) - 1, 0);\n    if (received <= 0) { return; }\n    request[received] = '\\0';"
    "\n    method[0] = '\\0';\n    path[0] = '\\0';\n    if (sscanf(request, \"%15s %1023s\", method, path) == 2) {\n        "
    "const LamoHttpRoute* route = lamo_http_find_route(path);\n        if (route) {\n            lamo_http_send_response(clie"
    "nt_socket, 200, route->body);\n            return;\n        }\n    }\n    lamo_http_send_response(client_socket, 404, \""
    "not found\");\n}\n\nstatic int lamo_http_run_server(int port, int serve_once) {\n    lamo_socket_t server_socket;\n    s"
    "truct sockaddr_in address;\n    int opt_value = 1;\n    /* GC Step 4: HTTP server loop runs gc_collect() every N request"
    "s so\n     * long-running http_serve() doesn't grow the arena without bound.\n     * The interval (100) is conservative "
    "\20024 typical Lamo HTTP programs\n     * handle tens of req/sec, so this adds ~1ms of GC overhead per\n     * second, w"
    "ell under the cost of a single request. */\n    static const int HTTP_GC_INTERVAL = 100;\n    long long request_count = "
    "0;\n    if (!lamo_http_init_runtime()) { return 0; }\n    server_socket = socket(AF_INET, SOCK_STREAM, 0);\n    if (serv"
    "er_socket == LAMO_INVALID_SOCKET) {\n        fprintf(stderr, \"failed to create HTTP socket\\n\");\n        return 0;\n "
    "   }\n    setsockopt(server_socket, SOL_SOCKET, SO_REUSEADDR, (const char*)&opt_value, sizeof(opt_value));\n    memset(&"
    "address, 0, sizeof(address));\n    address.sin_family = AF_INET;\n    address.sin_addr.s_addr = htonl(INADDR_LOOPBACK);"
    "\n    address.sin_port = htons((unsigned short)port);\n    if (bind(server_socket, (struct sockaddr*)&address, sizeof(ad"
    "dress)) != 0) {\n        fprintf(stderr, \"failed to bind HTTP server on port %d\\n\", port);\n        lamo_close_socket"
    "(server_socket);\n        return 0;\n    }\n    if (listen(server_socket, 8) != 0) {\n        fprintf(stderr, \"failed t"
    "o listen on HTTP server port %d\\n\", port);\n        lamo_close_socket(server_socket);\n        return 0;\n    }\n    w"
    "hile (1) {\n        lamo_socket_t client_socket = accept(server_socket, NULL, NULL);\n        if (client_socket == LAMO_"
    "INVALID_SOCKET) { break; }\n        lamo_http_handle_client(client_socket);\n        lamo_close_socket(client_socket);\n"
    "        if (serve_once) { break; }\n        /* Periodic GC: every HTTP_GC_INTERVAL requests, reclaim\n         * unreach"
    "able allocations. The route table (registered via\n         * http_route) lives in `lamo_http_routes`, which is a separa"
    "te\n         * malloc'd linked list NOT tracked by the GC \20024 so routes stay\n         * alive across collections. On"
    "ly per-request garbage (string\n         * buffers built while parsing/handling) gets swept. */\n        if (++request_c"
    "ount >= HTTP_GC_INTERVAL) {\n            request_count = 0;\n            lamo_gc_collect();\n        }\n    }\n    lamo_"
    "close_socket(server_socket);\n    return 1;\n}\n\n#endif /* LAMO_NEEDS_HTTP_RUNTIME */\n\n\n\n/* ======================="
    "=========================================== */\n/* 4. STD runtime (only emitted when the program uses std_* builtins) */"
    "\n/* ================================================================== */\n#ifdef LAMO_NEEDS_STD_RUNTIME\n\n/* Cross-pl"
    "atform headers. We try to keep this self-contained. */\n#include <sys/types.h>\n#include <sys/stat.h>\n#include <errno.h"
    ">\n\n#if defined(_WIN32)\n  #ifndef _WIN32_WINNT\n    #define _WIN32_WINNT 0x0600\n  #endif\n  /* Header order matters o"
    "n Windows: winsock2.h MUST come before windows.h\n   * to avoid the SOCKET vs. HANDLE redefinition conflict. We also inc"
    "lude\n   * <time.h> for time()/clock() and <process.h> for _getpid(). */\n  #include <winsock2.h>\n  #include <ws2tcpip."
    "h>\n  #include <windows.h>\n  #include <direct.h>\n  #include <io.h>\n  #include <process.h>\n  #include <time.h>\n  /* "
    "Note: we use the ANSI (char*) versions of the Windows file/env APIs\n   * rather than the wide-char (_w*) versions. The "
    "Lamo runtime stores all\n   * strings as UTF-8 char*, so passing them directly to the ANSI APIs is\n   * simpler and wor"
    "ks for ASCII paths (the common case). For full UTF-8\n   * path support on Windows, we'd need to convert to UTF-16 befor"
    "e each\n   * call \20024 that's future work. */\n  #define LAMO_PATH_SEP '\\\\'\n  #define LAMO_PATH_SEP_STR \"\\\\\"\n "
    " #define lamo_access _access\n  #define lamo_stat _stat\n  #define lamo_mkdir(path) _mkdir(path)\n  #define lamo_rmdir _"
    "rmdir\n  #define lamo_unlink _unlink\n  #define lamo_getenv getenv\n  #define lamo_setenv(name, val) _putenv_s(name, val"
    ")\n  #define lamo_unsetenv(name) _putenv_s(name, \"\")\n  typedef struct _stat lamo_stat_t;\n#else\n  #include <unistd.h"
    ">\n  #include <dirent.h>\n  #include <time.h>\n  #include <sys/wait.h>\n  #include <pthread.h>\n  #include <sys/socket.h"
    ">\n  #include <netinet/in.h>\n  #include <netdb.h>\n  #include <arpa/inet.h>\n  #define LAMO_PATH_SEP '/'\n  #define LAM"
    "O_PATH_SEP_STR \"/\"\n  #define lamo_access access\n  #define lamo_stat stat\n  #define lamo_mkdir(path) mkdir(path, 075"
    "5)\n  #define lamo_rmdir rmdir\n  #define lamo_unlink unlink\n  #define lamo_getenv getenv\n  #define lamo_setenv(name, "
    "val) setenv(name, val, 1)\n  #define lamo_unsetenv(name) unsetenv(name)\n  typedef struct stat lamo_stat_t;\n#endif\n\n/"
    "* Helper: convert C string (UTF-8) to wide string for Windows API.\n * On non-Windows, this is a no-op (returns NULL or "
    "the input). */\n#if defined(_WIN32)\nstatic LAMO_UNUSED wchar_t* lamo_utf8_to_wide(const char* utf8) {\n    if (!utf8) r"
    "eturn NULL;\n    int needed = MultiByteToWideChar(CP_UTF8, 0, utf8, -1, NULL, 0);\n    if (needed <= 0) return NULL;\n  "
    "  wchar_t* wide = (wchar_t*)malloc(sizeof(wchar_t) * needed);\n    if (!wide) return NULL;\n    MultiByteToWideChar(CP_U"
    "TF8, 0, utf8, -1, wide, needed);\n    return wide;\n}\nstatic LAMO_UNUSED char* lamo_wide_to_utf8(const wchar_t* wide) {"
    "\n    if (!wide) return NULL;\n    int needed = WideCharToMultiByte(CP_UTF8, 0, wide, -1, NULL, 0, NULL, NULL);\n    if "
    "(needed <= 0) return NULL;\n    char* utf8 = (char*)malloc(needed);\n    if (!utf8) return NULL;\n    WideCharToMultiByt"
    "e(CP_UTF8, 0, wide, -1, utf8, needed, NULL, NULL);\n    return utf8;\n}\n#endif\n\n/* Helper: build a LamoValue string f"
    "rom a heap-allocated UTF-8 buffer,\n * registering it in the arena so it's freed at exit. */\nstatic LAMO_UNUSED LamoVal"
    "ue lamo_std_string(char* heap_str) {\n    LamoValue v = lamo_make_string(heap_str ? heap_str : \"\");\n    if (heap_str)"
    " {\n        /* Register in arena so it's freed at exit. We re-arena-alloc\n         * and copy, then free the original. "
    "*/\n        char* arena_copy = lamo_arena_strdup(heap_str);\n        free(heap_str);\n        v.string_value = arena_cop"
    "y;\n    }\n    return v;\n}\n\n/* ================================================================== */\n/* std.math "
    "\20024 math functions beyond what the language offers natively */\n/* =================================================="
    "================ */\n\nstatic LAMO_UNUSED LamoValue lamo_math_sqrt(LamoValue v) {\n    return lamo_make_float(sqrt(lamo_"
    "as_float(v)));\n}\nstatic LAMO_UNUSED LamoValue lamo_math_pow(LamoValue base, LamoValue exp) {\n    return lamo_make_flo"
    "at(pow(lamo_as_float(base), lamo_as_float(exp)));\n}\nstatic LAMO_UNUSED LamoValue lamo_math_sin(LamoValue v) {\n    ret"
    "urn lamo_make_float(sin(lamo_as_float(v)));\n}\nstatic LAMO_UNUSED LamoValue lamo_math_cos(LamoValue v) {\n    return la"
    "mo_make_float(cos(lamo_as_float(v)));\n}\nstatic LAMO_UNUSED LamoValue lamo_math_tan(LamoValue v) {\n    return lamo_mak"
    "e_float(tan(lamo_as_float(v)));\n}\nstatic LAMO_UNUSED LamoValue lamo_math_floor(LamoValue v) {\n    return lamo_make_fl"
    "oat(floor(lamo_as_float(v)));\n}\nstatic LAMO_UNUSED LamoValue lamo_math_ceil(LamoValue v) {\n    return lamo_make_float"
    "(ceil(lamo_as_float(v)));\n}\nstatic LAMO_UNUSED LamoValue lamo_math_round(LamoValue v) {\n    return lamo_make_float(ro"
    "und(lamo_as_float(v)));\n}\nstatic LAMO_UNUSED LamoValue lamo_math_min(LamoValue a, LamoValue b) {\n    if (a.type == LA"
    "MO_VALUE_FLOAT || b.type == LAMO_VALUE_FLOAT) {\n        double x = lamo_as_float(a), y = lamo_as_float(b);\n        ret"
    "urn lamo_make_float(x < y ? x : y);\n    }\n    long long x = lamo_as_int(a), y = lamo_as_int(b);\n    return lamo_make_"
    "int(x < y ? x : y);\n}\nstatic LAMO_UNUSED LamoValue lamo_math_max(LamoValue a, LamoValue b) {\n    if (a.type == LAMO_V"
    "ALUE_FLOAT || b.type == LAMO_VALUE_FLOAT) {\n        double x = lamo_as_float(a), y = lamo_as_float(b);\n        return "
    "lamo_make_float(x > y ? x : y);\n    }\n    long long x = lamo_as_int(a), y = lamo_as_int(b);\n    return lamo_make_int("
    "x > y ? x : y);\n}\nstatic LAMO_UNUSED LamoValue lamo_math_clamp(LamoValue v, LamoValue lo, LamoValue hi) {\n    LamoVal"
    "ue result = v;\n    if (lamo_is_truthy(lamo_less(v, lo))) result = lo;\n    else if (lamo_is_truthy(lamo_greater(v, hi))"
    ") result = hi;\n    return result;\n}\n\n/* ================================================================== */\n/* st"
    "d.string \20024 UTF-8 string operations                                */\n/* =========================================="
    "======================== */\n\n/* Byte length (UTF-8 bytes, not codepoint count \20024 matches what most\n * users expec"
    "t for file I/O and string slicing at the byte level). */\nstatic LAMO_UNUSED LamoValue lamo_str_length(LamoValue v) {\n "
    "   const char* s = lamo_as_cstring(v);\n    return lamo_make_int((long long)strlen(s));\n}\n\nstatic LAMO_UNUSED LamoVal"
    "ue lamo_str_upper(LamoValue v) {\n    const char* s = lamo_as_cstring(v);\n    size_t n = strlen(s);\n    char* out = (c"
    "har*)lamo_arena_alloc(n + 1);\n    size_t i;\n    for (i = 0; i < n; i++) {\n        unsigned char c = (unsigned char)s["
    "i];\n        /* ASCII fast path; non-ASCII bytes are preserved unchanged. */\n        if (c >= 'a' && c <= 'z') out[i] ="
    " (char)(c - 32);\n        else out[i] = s[i];\n    }\n    out[n] = '\\0';\n    return lamo_make_string(out);\n}\n\nstati"
    "c LAMO_UNUSED LamoValue lamo_str_lower(LamoValue v) {\n    const char* s = lamo_as_cstring(v);\n    size_t n = strlen(s)"
    ";\n    char* out = (char*)lamo_arena_alloc(n + 1);\n    size_t i;\n    for (i = 0; i < n; i++) {\n        unsigned char "
    "c = (unsigned char)s[i];\n        if (c >= 'A' && c <= 'Z') out[i] = (char)(c + 32);\n        else out[i] = s[i];\n    }"
    "\n    out[n] = '\\0';\n    return lamo_make_string(out);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_str_starts_with(LamoVal"
    "ue s, LamoValue prefix) {\n    const char* a = lamo_as_cstring(s);\n    const char* b = lamo_as_cstring(prefix);\n    si"
    "ze_t la = strlen(a), lb = strlen(b);\n    if (lb > la) return lamo_make_bool(0);\n    return lamo_make_bool(strncmp(a, b"
    ", lb) == 0);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_str_ends_with(LamoValue s, LamoValue suffix) {\n    const char* a ="
    " lamo_as_cstring(s);\n    const char* b = lamo_as_cstring(suffix);\n    size_t la = strlen(a), lb = strlen(b);\n    if ("
    "lb > la) return lamo_make_bool(0);\n    return lamo_make_bool(strcmp(a + (la - lb), b) == 0);\n}\n\nstatic LAMO_UNUSED L"
    "amoValue lamo_str_contains(LamoValue s, LamoValue needle) {\n    const char* a = lamo_as_cstring(s);\n    const char* b "
    "= lamo_as_cstring(needle);\n    if (!*b) return lamo_make_bool(1);\n    return lamo_make_bool(strstr(a, b) != NULL);\n}"
    "\n\nstatic LAMO_UNUSED LamoValue lamo_str_index_of(LamoValue s, LamoValue needle) {\n    const char* a = lamo_as_cstring"
    "(s);\n    const char* b = lamo_as_cstring(needle);\n    const char* p = strstr(a, b);\n    if (!p) return lamo_make_int("
    "-1);\n    return lamo_make_int((long long)(p - a));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_str_trim(LamoValue v) {\n   "
    " const char* s = lamo_as_cstring(v);\n    const char* start = s;\n    const char* end = s + strlen(s);\n    while (start"
    " < end) {\n        unsigned char c = (unsigned char)*start;\n        if (c == ' ' || c == '\\t' || c == '\\n' || c == '"
    "\\r' || c == '\\v' || c == '\\f')\n            start++;\n        else break;\n    }\n    while (end > start) {\n        "
    "unsigned char c = (unsigned char)end[-1];\n        if (c == ' ' || c == '\\t' || c == '\\n' || c == '\\r' || c == '\\v' "
    "|| c == '\\f')\n            end--;\n        else break;\n    }\n    {\n        size_t len = (size_t)(end - start);\n    "
    "    char* out = (char*)lamo_arena_alloc(len + 1);\n        memcpy(out, start, len);\n        out[len] = '\\0';\n        "
    "return lamo_make_string(out);\n    }\n}\n\nstatic LAMO_UNUSED LamoValue lamo_str_substring(LamoValue s, LamoValue start_"
    "v, LamoValue end_v) {\n    const char* str = lamo_as_cstring(s);\n    long long len = (long long)strlen(str);\n    long "
    "long start = lamo_as_int(start_v);\n    long long end;\n    size_t out_len;\n    char* out;\n    if (start < 0) start +="
    " len;\n    if (start < 0) start = 0;\n    if (start > len) start = len;\n    /* If end_v is negative, treat as \"to end "
    "of string\". */\n    if (end_v.type == LAMO_VALUE_INT && end_v.int_value < 0) {\n        end = len;\n    } else {\n     "
    "   end = lamo_as_int(end_v);\n        if (end < 0) end += len;\n        if (end < 0) end = 0;\n        if (end > len) en"
    "d = len;\n    }\n    if (end < start) end = start;\n    out_len = (size_t)(end - start);\n    out = (char*)lamo_arena_al"
    "loc(out_len + 1);\n    memcpy(out, str + start, out_len);\n    out[out_len] = '\\0';\n    return lamo_make_string(out);"
    "\n}\n\nstatic LAMO_UNUSED LamoValue lamo_str_replace(LamoValue s, LamoValue from, LamoValue to) {\n    const char* src ="
    " lamo_as_cstring(s);\n    const char* from_s = lamo_as_cstring(from);\n    const char* to_s = lamo_as_cstring(to);\n    "
    "size_t from_len = strlen(from_s);\n    size_t to_len = strlen(to_s);\n    size_t src_len = strlen(src);\n    if (from_le"
    "n == 0) {\n        /* Empty \"from\" \20024 return original. */\n        return lamo_make_string(lamo_arena_strdup(src))"
    ";\n    }\n    /* Count occurrences. */\n    size_t count = 0;\n    const char* p = src;\n    while ((p = strstr(p, from_"
    "s)) != NULL) {\n        count++;\n        p += from_len;\n    }\n    /* Allocate output. */\n    size_t out_len = src_le"
    "n + count * (to_len > from_len ? to_len - from_len : 0);\n    /* If to_len < from_len, out shrinks; the above count hand"
    "les growth,\n     * for shrinkage we just use src_len as upper bound. */\n    if (to_len < from_len) out_len = src_len;"
    "\n    char* out = (char*)lamo_arena_alloc(out_len + 1);\n    char* dst = out;\n    p = src;\n    while (*p) {\n        i"
    "f (strncmp(p, from_s, from_len) == 0) {\n            memcpy(dst, to_s, to_len);\n            dst += to_len;\n           "
    " p += from_len;\n        } else {\n            *dst++ = *p++;\n        }\n    }\n    *dst = '\\0';\n    return lamo_make"
    "_string(out);\n}\n\n/* split(string, sep) -> array of strings. Empty separator splits into\n * individual bytes (UTF-8 s"
    "afe at byte level). */\nstatic LAMO_UNUSED LamoValue lamo_str_split(LamoValue s, LamoValue sep) {\n    const char* src ="
    " lamo_as_cstring(s);\n    const char* sep_s = lamo_as_cstring(sep);\n    size_t sep_len = strlen(sep_s);\n    LamoArray*"
    " arr = lamo_array_alloc(4);\n    LamoValue result;\n    const char* start = src;\n    const char* p = src;\n    if (sep_"
    "len == 0) {\n        /* Split into individual bytes. */\n        size_t i;\n        for (i = 0; src[i] != '\\0'; i++) {"
    "\n            char* b = (char*)lamo_arena_alloc(2);\n            b[0] = src[i];\n            b[1] = '\\0';\n            "
    "lamo_array_push(lamo_make_array(arr), lamo_make_string(b));\n        }\n        return lamo_make_array(arr);\n    }\n   "
    " while ((p = strstr(p, sep_s)) != NULL) {\n        size_t len = (size_t)(p - start);\n        char* piece = (char*)lamo_"
    "arena_alloc(len + 1);\n        memcpy(piece, start, len);\n        piece[len] = '\\0';\n        lamo_array_push(lamo_mak"
    "e_array(arr), lamo_make_string(piece));\n        p += sep_len;\n        start = p;\n    }\n    /* Trailing piece. */\n  "
    "  {\n        size_t len = strlen(start);\n        char* piece = (char*)lamo_arena_alloc(len + 1);\n        memcpy(piece,"
    " start, len);\n        piece[len] = '\\0';\n        lamo_array_push(lamo_make_array(arr), lamo_make_string(piece));\n   "
    " }\n    result = lamo_make_array(arr);\n    (void)result;\n    return lamo_make_array(arr);\n}\n\nstatic LAMO_UNUSED Lam"
    "oValue lamo_str_char_at(LamoValue s, LamoValue idx) {\n    const char* str = lamo_as_cstring(s);\n    long long i = lamo"
    "_as_int(idx);\n    long long len = (long long)strlen(str);\n    if (i < 0) i += len;\n    if (i < 0 || i >= len) return "
    "lamo_make_string(\"\");\n    {\n        char* b = (char*)lamo_arena_alloc(2);\n        b[0] = str[i];\n        b[1] = '"
    "\\0';\n        return lamo_make_string(b);\n    }\n}\n\nstatic LAMO_UNUSED LamoValue lamo_str_repeat(LamoValue s, LamoVa"
    "lue n) {\n    const char* src = lamo_as_cstring(s);\n    long long count = lamo_as_int(n);\n    size_t src_len = strlen("
    "src);\n    if (count < 0) count = 0;\n    size_t out_len = src_len * (size_t)count;\n    char* out = (char*)lamo_arena_a"
    "lloc(out_len + 1);\n    size_t i;\n    out[0] = '\\0';\n    for (i = 0; i < (size_t)count; i++) {\n        memcpy(out + "
    "i * src_len, src, src_len);\n    }\n    out[out_len] = '\\0';\n    return lamo_make_string(out);\n}\n\n/* =============="
    "==================================================== */\n/* std.path \20024 path manipulation (string-based, cross-platf"
    "orm)         */\n/* ================================================================== */\n\nstatic LAMO_UNUSED LamoValu"
    "e lamo_path_join(LamoValue a, LamoValue b) {\n    const char* p = lamo_as_cstring(a);\n    const char* q = lamo_as_cstri"
    "ng(b);\n    size_t plen = strlen(p);\n    size_t qlen = strlen(q);\n    int need_sep = plen > 0 && p[plen-1] != '/' && p"
    "[plen-1] != '\\\\' && qlen > 0 && q[0] != '/' && q[0] != '\\\\';\n    char* out = (char*)lamo_arena_alloc(plen + (size_t"
    ")need_sep + qlen + 1);\n    memcpy(out, p, plen);\n    if (need_sep) out[plen] = '/';\n    memcpy(out + plen + (size_t)n"
    "eed_sep, q, qlen);\n    out[plen + (size_t)need_sep + qlen] = '\\0';\n    return lamo_make_string(out);\n}\n\nstatic LAM"
    "O_UNUSED LamoValue lamo_path_parent(LamoValue v) {\n    const char* p = lamo_as_cstring(v);\n    const char* last_sep = "
    "NULL;\n    const char* q;\n    for (q = p; *q; q++) {\n        if (*q == '/' || *q == '\\\\') last_sep = q;\n    }\n    "
    "if (!last_sep) return lamo_make_string(\"\");\n    {\n        size_t len = (size_t)(last_sep - p);\n        char* out = "
    "(char*)lamo_arena_alloc(len + 1);\n        memcpy(out, p, len);\n        out[len] = '\\0';\n        return lamo_make_str"
    "ing(out);\n    }\n}\n\nstatic LAMO_UNUSED LamoValue lamo_path_filename(LamoValue v) {\n    const char* p = lamo_as_cstri"
    "ng(v);\n    const char* last_sep = NULL;\n    const char* q;\n    for (q = p; *q; q++) {\n        if (*q == '/' || *q =="
    " '\\\\') last_sep = q;\n    }\n    if (!last_sep) return lamo_make_string(lamo_arena_strdup(p));\n    return lamo_make_s"
    "tring(lamo_arena_strdup(last_sep + 1));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_path_extension(LamoValue v) {\n    const"
    " char* p = lamo_as_cstring(v);\n    const char* last_dot = NULL;\n    const char* last_sep = NULL;\n    const char* q;\n"
    "    for (q = p; *q; q++) {\n        if (*q == '/' || *q == '\\\\') { last_sep = q; last_dot = NULL; }\n        else if ("
    "*q == '.') last_dot = q;\n    }\n    if (!last_dot || last_dot == p || (last_sep && last_dot < last_sep)) return lamo_ma"
    "ke_string(\"\");\n    return lamo_make_string(lamo_arena_strdup(last_dot));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_path"
    "_absolute(LamoValue v) {\n#if defined(_WIN32)\n    char abs_path[MAX_PATH];\n    DWORD len = GetFullPathNameA(lamo_as_cs"
    "tring(v), MAX_PATH, abs_path, NULL);\n    if (len == 0 || len >= MAX_PATH) {\n        return lamo_make_string(lamo_arena"
    "_strdup(lamo_as_cstring(v)));\n    }\n    return lamo_std_string(lamo_heap_strdup(abs_path));\n#else\n    const char* p "
    "= lamo_as_cstring(v);\n    if (p[0] == '/') return lamo_make_string(lamo_arena_strdup(p));\n    {\n        char cwd[4096"
    "];\n        if (getcwd(cwd, sizeof(cwd)) == NULL) {\n            return lamo_make_string(lamo_arena_strdup(p));\n       "
    " }\n        size_t clen = strlen(cwd);\n        size_t plen = strlen(p);\n        int need_sep = clen > 0 && cwd[clen-1]"
    " != '/';\n        char* out = (char*)lamo_arena_alloc(clen + (size_t)need_sep + plen + 1);\n        memcpy(out, cwd, cle"
    "n);\n        if (need_sep) out[clen] = '/';\n        memcpy(out + clen + (size_t)need_sep, p, plen);\n        out[clen +"
    " (size_t)need_sep + plen] = '\\0';\n        return lamo_make_string(out);\n    }\n#endif\n}\n\nstatic LAMO_UNUSED LamoVa"
    "lue lamo_path_normalize(LamoValue v) {\n    /* Simple normalization: collapse multiple separators, resolve \".\"\n     *"
    " and \"..\" segments. Does NOT touch the filesystem.\n     * Algorithm: split on '/', then walk segments with a stack. *"
    "/\n    const char* p = lamo_as_cstring(v);\n    size_t plen = strlen(p);\n    /* First pass: collapse repeated separator"
    "s and build a clean\n     * forward-slash path. */\n    char* cleaned = (char*)lamo_arena_alloc(plen + 2);\n    size_t c"
    "i = 0;\n    int is_absolute = (plen > 0 && (p[0] == '/' || p[0] == '\\\\'));\n    if (is_absolute) { cleaned[ci++] = '/'"
    "; }\n    size_t i = 0;\n    /* Skip leading separators. */\n    while (i < plen && (p[i] == '/' || p[i] == '\\\\')) i++;"
    "\n    int last_was_sep = is_absolute;\n    while (i < plen) {\n        char c = p[i];\n        if (c == '/' || c == '"
    "\\\\') {\n            if (!last_was_sep) {\n                cleaned[ci++] = '/';\n                last_was_sep = 1;\n   "
    "         }\n        } else {\n            cleaned[ci++] = c;\n            last_was_sep = 0;\n        }\n        i++;\n  "
    "  }\n    /* Strip trailing separator (unless it's the root). */\n    if (ci > 1 && cleaned[ci-1] == '/') ci--;\n    clea"
    "ned[ci] = '\\0';\n\n    /* Second pass: split into segments and apply . and .. resolution. */\n    /* seg_start[i] / seg"
    "_end[i] are offsets into cleaned. */\n    size_t seg_start[256];\n    size_t seg_end[256];\n    int seg_count = 0;\n    "
    "size_t j = is_absolute ? 1 : 0;\n    while (j < ci) {\n        size_t start = j;\n        while (j < ci && cleaned[j] !="
    " '/') j++;\n        size_t end = j;\n        /* Skip the separator. */\n        if (j < ci) j++;\n        /* Process the"
    " segment. */\n        size_t seg_len = end - start;\n        if (seg_len == 1 && cleaned[start] == '.') {\n            /"
    "* Skip. */\n        } else if (seg_len == 2 && cleaned[start] == '.' && cleaned[start+1] == '.') {\n            /* Pop t"
    "he last segment if any. */\n            if (seg_count > 0) seg_count--;\n        } else if (seg_len > 0) {\n            "
    "if (seg_count < 256) {\n                seg_start[seg_count] = start;\n                seg_end[seg_count] = end;\n      "
    "          seg_count++;\n            }\n        }\n    }\n\n    /* Rebuild. */\n    size_t total = is_absolute ? 1 : 0;\n"
    "    int k;\n    for (k = 0; k < seg_count; k++) {\n        total += (seg_end[k] - seg_start[k]) + (k > 0 || is_absolute "
    "? 1 : 0);\n    }\n    char* out = (char*)lamo_arena_alloc(total + 1);\n    size_t oi = 0;\n    if (is_absolute) { out[oi"
    "++] = '/'; }\n    for (k = 0; k < seg_count; k++) {\n        if (k > 0) out[oi++] = '/';\n        size_t len = seg_end[k"
    "] - seg_start[k];\n        memcpy(out + oi, cleaned + seg_start[k], len);\n        oi += len;\n    }\n    out[oi] = '\\0"
    "';\n    /* Handle empty result: should be \"/\" if absolute, \"\" otherwise. */\n    if (oi == 0) {\n        if (is_abso"
    "lute) {\n            out[0] = '/';\n            out[1] = '\\0';\n        } else {\n            out[0] = '\\0';\n        "
    "}\n    }\n    return lamo_make_string(out);\n}\n\n/* ================================================================== "
    "*/\n/* std.fs \20024 file system operations                                     */\n/* ================================="
    "================================= */\n\nstatic LAMO_UNUSED LamoValue lamo_fs_exists(LamoValue path) {\n#if defined(_WIN3"
    "2)\n    int r = lamo_access(lamo_as_cstring(path), 0);\n#else\n    int r = access(lamo_as_cstring(path), F_OK);\n#endif"
    "\n    return lamo_make_bool(r == 0);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs_is_file(LamoValue path) {\n    lamo_stat"
    "_t st;\n#if defined(_WIN32)\n    int r = lamo_stat(lamo_as_cstring(path), &st);\n#else\n    int r = stat(lamo_as_cstring"
    "(path), &st);\n#endif\n    if (r != 0) return lamo_make_bool(0);\n    return lamo_make_bool(S_ISREG(st.st_mode));\n}\n\n"
    "static LAMO_UNUSED LamoValue lamo_fs_is_dir(LamoValue path) {\n    lamo_stat_t st;\n#if defined(_WIN32)\n    int r = lam"
    "o_stat(lamo_as_cstring(path), &st);\n#else\n    int r = stat(lamo_as_cstring(path), &st);\n#endif\n    if (r != 0) retur"
    "n lamo_make_bool(0);\n    return lamo_make_bool(S_ISDIR(st.st_mode));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs_read_te"
    "xt(LamoValue path) {\n    FILE* f;\n    long size;\n    char* buf;\n    size_t nread;\nf = fopen(lamo_as_cstring(path), "
    "\"rb\");\n    if (!f) {\n        fprintf(stderr, \"runtime error: fs.readText: cannot open '%s'\\n\", lamo_as_cstring(pa"
    "th));\n        return lamo_make_string(\"\");\n    }\n    fseek(f, 0, SEEK_END);\n    size = ftell(f);\n    fseek(f, 0, "
    "SEEK_SET);\n    if (size < 0) { fclose(f); return lamo_make_string(\"\"); }\n    buf = (char*)lamo_arena_alloc((size_t)s"
    "ize + 1);\n    nread = fread(buf, 1, (size_t)size, f);\n    fclose(f);\n    buf[nread] = '\\0';\n    return lamo_make_st"
    "ring(buf);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs_write_text(LamoValue path, LamoValue content) {\n    FILE* f;\n   "
    " const char* s = lamo_as_cstring(content);\n    size_t len = strlen(s);\n    size_t written;\nf = fopen(lamo_as_cstring("
    "path), \"wb\");\n    if (!f) {\n        fprintf(stderr, \"runtime error: fs.writeText: cannot open '%s'\\n\", lamo_as_cs"
    "tring(path));\n        return lamo_make_int(0);\n    }\n    written = fwrite(s, 1, len, f);\n    fclose(f);\n    return "
    "lamo_make_int(written == len ? 1 : 0);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs_append_text(LamoValue path, LamoValue "
    "content) {\n    FILE* f;\n    const char* s = lamo_as_cstring(content);\n    size_t len = strlen(s);\n    size_t written"
    ";\nf = fopen(lamo_as_cstring(path), \"ab\");\n    if (!f) return lamo_make_int(0);\n    written = fwrite(s, 1, len, f);"
    "\n    fclose(f);\n    return lamo_make_int(written == len ? 1 : 0);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs_delete(La"
    "moValue path) {\n#if defined(_WIN32)\n    int r = lamo_unlink(lamo_as_cstring(path));\n#else\n    int r = unlink(lamo_as"
    "_cstring(path));\n#endif\n    return lamo_make_int(r == 0 ? 1 : 0);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs_create_di"
    "r(LamoValue path) {\n#if defined(_WIN32)\n    int r = lamo_mkdir(lamo_as_cstring(path));\n#else\n    int r = mkdir(lamo_"
    "as_cstring(path), 0755);\n#endif\n    return lamo_make_int(r == 0 ? 1 : 0);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs_r"
    "emove_dir(LamoValue path) {\n#if defined(_WIN32)\n    int r = lamo_rmdir(lamo_as_cstring(path));\n#else\n    int r = rmd"
    "ir(lamo_as_cstring(path));\n#endif\n    return lamo_make_int(r == 0 ? 1 : 0);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs"
    "_copy(LamoValue src, LamoValue dst) {\n    FILE* in;\n    FILE* out;\n    int c;\nin = fopen(lamo_as_cstring(src), \"rb"
    "\");\n    if (!in) return lamo_make_int(0);\nout = fopen(lamo_as_cstring(dst), \"wb\");\n    if (!out) { fclose(in); ret"
    "urn lamo_make_int(0); }\n    while ((c = fgetc(in)) != EOF) fputc(c, out);\n    fclose(in);\n    fclose(out);\n    retur"
    "n lamo_make_int(1);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_fs_move(LamoValue src, LamoValue dst) {\n#if defined(_WIN32)"
    "\n    {\n        int r = MoveFileA(lamo_as_cstring(src), lamo_as_cstring(dst));\n        return lamo_make_int(r ? 1 : 0)"
    ";\n    }\n#else\n    if (rename(lamo_as_cstring(src), lamo_as_cstring(dst)) == 0)\n        return lamo_make_int(1);\n   "
    " /* Fall back to copy + delete if rename fails (cross-device). */\n    if (lamo_is_truthy(lamo_fs_copy(src, dst))) {\n  "
    "      lamo_fs_delete(src);\n        return lamo_make_int(1);\n    }\n    return lamo_make_int(0);\n#endif\n}\n\nstatic L"
    "AMO_UNUSED LamoValue lamo_fs_list_files(LamoValue path) {\n    LamoArray* arr = lamo_array_alloc(8);\n#if defined(_WIN32"
    ")\n    {\n        char pattern[1024];\n        WIN32_FIND_DATAA find_data;\n        HANDLE h;\n        char* dir = (char"
    "*)lamo_as_cstring(path);\n        snprintf(pattern, sizeof(pattern), \"%s\\\\*\", dir);\n        {\n            h = Find"
    "FirstFileA(pattern, &find_data);\n        }\n        if (h == INVALID_HANDLE_VALUE) return lamo_make_array(arr);\n      "
    "  do {\n            char* name;\n            if (strcmp(find_data.cFileName, \".\") == 0 ||\n                strcmp(find"
    "_data.cFileName, \"..\") == 0) continue;\n            name = lamo_heap_strdup(find_data.cFileName);\n            if (nam"
    "e) {\n                lamo_array_push(lamo_make_array(arr), lamo_std_string(name));\n            }\n        } while (Fin"
    "dNextFileA(h, &find_data));\n        FindClose(h);\n    }\n#else\n    {\n        DIR* d = opendir(lamo_as_cstring(path))"
    ";\n        if (!d) return lamo_make_array(arr);\n        struct dirent* entry;\n        while ((entry = readdir(d)) != N"
    "ULL) {\n            if (strcmp(entry->d_name, \".\") == 0 || strcmp(entry->d_name, \"..\") == 0) continue;\n            "
    "lamo_array_push(lamo_make_array(arr),\n                            lamo_make_string(lamo_arena_strdup(entry->d_name)));"
    "\n        }\n        closedir(d);\n    }\n#endif\n    return lamo_make_array(arr);\n}\n\nstatic LAMO_UNUSED LamoValue la"
    "mo_fs_size(LamoValue path) {\n    lamo_stat_t st;\n#if defined(_WIN32)\n    int r = lamo_stat(lamo_as_cstring(path), &st"
    ");\n#else\n    int r = stat(lamo_as_cstring(path), &st);\n#endif\n    if (r != 0) return lamo_make_int(-1);\n    return "
    "lamo_make_int((long long)st.st_size);\n}\n\n/* ================================================================== */\n/*"
    " std.env \20024 environment variables                                     */\n/* ======================================="
    "=========================== */\n\nstatic LAMO_UNUSED LamoValue lamo_env_get(LamoValue name) {\n    const char* v = lamo_"
    "getenv(lamo_as_cstring(name));\n    if (!v) return lamo_make_string(\"\");\n    return lamo_make_string(lamo_arena_strdu"
    "p(v));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_env_set(LamoValue name, LamoValue value) {\n#if defined(_WIN32)\n    int "
    "r = _putenv_s(lamo_as_cstring(name), lamo_as_cstring(value));\n    return lamo_make_int(r == 0 ? 1 : 0);\n#else\n    ret"
    "urn lamo_make_int(setenv(lamo_as_cstring(name), lamo_as_cstring(value), 1) == 0 ? 1 : 0);\n#endif\n}\n\nstatic LAMO_UNUS"
    "ED LamoValue lamo_env_remove(LamoValue name) {\n#if defined(_WIN32)\n    /* On Windows, setting to empty achieves the \""
    "remove\" effect. */\n    return lamo_env_set(name, lamo_make_string(\"\"));\n#else\n    return lamo_make_int(unsetenv(la"
    "mo_as_cstring(name)) == 0 ? 1 : 0);\n#endif\n}\n\n/* ================================================================== "
    "*/\n/* std.os \20024 operating system info                                      */\n/* ================================="
    "================================= */\n\nstatic LAMO_UNUSED LamoValue lamo_os_name(void) {\n#if defined(_WIN32)\n    retu"
    "rn lamo_make_string(lamo_arena_strdup(\"windows\"));\n#elif defined(__APPLE__)\n    return lamo_make_string(lamo_arena_s"
    "trdup(\"macos\"));\n#elif defined(__linux__)\n    return lamo_make_string(lamo_arena_strdup(\"linux\"));\n#elif defined("
    "__FreeBSD__)\n    return lamo_make_string(lamo_arena_strdup(\"freebsd\"));\n#elif defined(__OpenBSD__)\n    return lamo_"
    "make_string(lamo_arena_strdup(\"openbsd\"));\n#else\n    return lamo_make_string(lamo_arena_strdup(\"unknown\"));\n#endi"
    "f\n}\n\nstatic LAMO_UNUSED LamoValue lamo_os_arch(void) {\n#if defined(_WIN64) || defined(__x86_64__) || defined(__ppc64"
    "__) || defined(__aarch64__)\n    return lamo_make_string(lamo_arena_strdup(\"x64\"));\n#elif defined(_WIN32) || defined("
    "__i386__) || defined(__i486__) || defined(__i586__) || defined(__i686__)\n    return lamo_make_string(lamo_arena_strdup("
    "\"x86\"));\n#elif defined(__arm__)\n    return lamo_make_string(lamo_arena_strdup(\"arm\"));\n#elif defined(__aarch64__)"
    "\n    return lamo_make_string(lamo_arena_strdup(\"arm64\"));\n#else\n    return lamo_make_string(lamo_arena_strdup(\"unk"
    "nown\"));\n#endif\n}\n\nstatic LAMO_UNUSED LamoValue lamo_os_cpu_count(void) {\n#if defined(_WIN32)\n    SYSTEM_INFO inf"
    "o;\n    GetSystemInfo(&info);\n    return lamo_make_int((long long)info.dwNumberOfProcessors);\n#else\n    long n = sysc"
    "onf(_SC_NPROCESSORS_ONLN);\n    return lamo_make_int(n > 0 ? n : 1);\n#endif\n}\n\nstatic LAMO_UNUSED LamoValue lamo_os_"
    "home(void) {\n#if defined(_WIN32)\n    const char* h = getenv(\"USERPROFILE\");\n    if (!h) h = getenv(\"HOMEPATH\");\n"
    "    if (!h) h = \"C:\\\\\";\n    return lamo_make_string(lamo_arena_strdup(h));\n#else\n    const char* h = getenv(\"HOM"
    "E\");\n    if (!h) h = \"/\";\n    return lamo_make_string(lamo_arena_strdup(h));\n#endif\n}\n\nstatic LAMO_UNUSED LamoV"
    "alue lamo_os_temp_dir(void) {\n#if defined(_WIN32)\n    char buf[MAX_PATH];\n    DWORD len = GetTempPathA(MAX_PATH, buf)"
    ";\n    if (len == 0 || len >= MAX_PATH) return lamo_make_string(lamo_arena_strdup(\".\"));\n    /* Strip trailing backsl"
    "ash for consistency. */\n    if (len > 0 && buf[len-1] == '\\\\') buf[len-1] = '\\0';\n    return lamo_make_string(lamo_"
    "arena_strdup(buf));\n#else\n    const char* t = getenv(\"TMPDIR\");\n    if (!t) t = \"/tmp\";\n    return lamo_make_str"
    "ing(lamo_arena_strdup(t));\n#endif\n}\n\n/* ================================================================== */\n/* st"
    "d.time \20024 time functions                                           */\n/* =========================================="
    "======================== */\n\nstatic LAMO_UNUSED LamoValue lamo_time_now(void) {\n    /* Returns current time in millis"
    "econds since epoch. */\n#if defined(_WIN32)\n    FILETIME ft;\n    ULARGE_INTEGER li;\n    GetSystemTimeAsFileTime(&ft);"
    "\n    li.LowPart = ft.dwLowDateTime;\n    li.HighPart = ft.dwHighDateTime;\n    /* Convert from 100ns intervals since 16"
    "01 to ms since 1970. */\n    return lamo_make_int((long long)((li.QuadPart - 116444736000000000ULL) / 10000ULL));\n#else"
    "\n    struct timespec ts;\n    clock_gettime(CLOCK_REALTIME, &ts);\n    return lamo_make_int((long long)ts.tv_sec * 1000"
    "LL + (long long)ts.tv_nsec / 1000000LL);\n#endif\n}\n\nstatic LAMO_UNUSED LamoValue lamo_time_timestamp(void) {\n    /* "
    "Seconds since epoch. */\n    return lamo_make_int((long long)time(NULL));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_time_s"
    "leep(LamoValue ms) {\n    long long millis = lamo_as_int(ms);\n    if (millis < 0) millis = 0;\n#if defined(_WIN32)\n   "
    " Sleep((DWORD)millis);\n#else\n    {\n        struct timespec ts;\n        ts.tv_sec = millis / 1000;\n        ts.tv_nse"
    "c = (millis % 1000) * 1000000L;\n        nanosleep(&ts, NULL);\n    }\n#endif\n    return lamo_make_int(0);\n}\n\n/* Mon"
    "otonic clock for elapsed() measurements. */\nstatic LAMO_UNUSED LamoValue lamo_time_monotonic(void) {\n#if defined(_WIN3"
    "2)\n    static LARGE_INTEGER freq = {0};\n    LARGE_INTEGER counter;\n    if (freq.QuadPart == 0) QueryPerformanceFreque"
    "ncy(&freq);\n    QueryPerformanceCounter(&counter);\n    /* Convert to milliseconds. */\n    return lamo_make_int((long "
    "long)(counter.QuadPart * 1000 / freq.QuadPart));\n#else\n    struct timespec ts;\n    clock_gettime(CLOCK_MONOTONIC, &ts"
    ");\n    return lamo_make_int((long long)ts.tv_sec * 1000LL + (long long)ts.tv_nsec / 1000000LL);\n#endif\n}\n\n/* ======"
    "============================================================ */\n/* std.process \20024 process operations               "
    "                     */\n/* ================================================================== */\n\nstatic LAMO_UNUSED "
    "LamoValue lamo_process_pid(void) {\n#if defined(_WIN32)\n    return lamo_make_int((long long)GetCurrentProcessId());\n#e"
    "lse\n    return lamo_make_int((long long)getpid());\n#endif\n}\n\n/* run(cmd) \20024 execute a shell command, return exi"
    "t code. */\nstatic LAMO_UNUSED LamoValue lamo_process_run(LamoValue cmd) {\n    int r = system(lamo_as_cstring(cmd));\n "
    "   return lamo_make_int((long long)r);\n}\n\n/* exec(cmd) \20024 execute a shell command, capture stdout as string. */\n"
    "static LAMO_UNUSED LamoValue lamo_process_exec(LamoValue cmd) {\n    FILE* pipe;\n    char* result = NULL;\n    size_t r"
    "esult_len = 0;\n    size_t result_cap = 0;\n    char buf[1024];\n#if defined(_WIN32)\n    pipe = _popen(lamo_as_cstring("
    "cmd), \"rb\");\n#else\n    pipe = popen(lamo_as_cstring(cmd), \"r\");\n#endif\n    if (!pipe) return lamo_make_string(la"
    "mo_arena_strdup(\"\"));\n    while (fgets(buf, sizeof(buf), pipe) != NULL) {\n        size_t chunk = strlen(buf);\n     "
    "   if (result_len + chunk + 1 > result_cap) {\n            while (result_len + chunk + 1 > result_cap) {\n              "
    "  result_cap = result_cap ? result_cap * 2 : 256;\n            }\n            {\n                char* new_result = (cha"
    "r*)realloc(result, result_cap);\n                if (!new_result) { free(result); result = NULL; break; }\n             "
    "   result = new_result;\n            }\n        }\n        if (result) {\n            memcpy(result + result_len, buf, c"
    "hunk);\n            result_len += chunk;\n            result[result_len] = '\\0';\n        }\n    }\n#if defined(_WIN32)"
    "\n    _pclose(pipe);\n#else\n    pclose(pipe);\n#endif\n    if (!result) return lamo_make_string(lamo_arena_strdup(\"\")"
    ");\n    {\n        LamoValue v = lamo_std_string(result);\n        return v;\n    }\n}\n\n/* exit_code() - return 0 alwa"
    "ys, the actual exit is handled by the\n * existing `exit(n)` builtin. Provided for symmetry with the API spec. */\nstati"
    "c LAMO_UNUSED LamoValue lamo_process_exit(LamoValue code) {\n    exit((int)lamo_as_int(code));\n    return lamo_make_int"
    "(0);\n}\n\n/* ================================================================== */\n/* std.random \20024 random number "
    "generation                               */\n/* ================================================================== */\n"
    "\n/* Simple xorshift64* PRNG, seeded from time + pid. Not cryptographic. */\nstatic unsigned long long lamo_random_state"
    " = 0;\nstatic int lamo_random_seeded = 0;\n\nstatic LAMO_UNUSED void lamo_random_seed_if_needed(void) {\n    if (!lamo_r"
    "andom_seeded) {\n        lamo_random_state = (unsigned long long)time(NULL);\n        lamo_random_state ^= (unsigned lon"
    "g long)\n#if defined(_WIN32)\n            GetCurrentProcessId();\n#else\n            getpid();\n#endif\n        lamo_ran"
    "dom_state |= 1; /* ensure non-zero */\n        lamo_random_seeded = 1;\n    }\n}\n\nstatic LAMO_UNUSED LamoValue lamo_ra"
    "ndom_seed(LamoValue s) {\n    lamo_random_state = (unsigned long long)lamo_as_int(s);\n    if (lamo_random_state == 0) l"
    "amo_random_state = 1;\n    lamo_random_seeded = 1;\n    return lamo_make_int(0);\n}\n\nstatic LAMO_UNUSED unsigned long "
    "long lamo_random_next(void) {\n    unsigned long long x = lamo_random_state;\n    x ^= x << 13;\n    x ^= x >> 7;\n    x"
    " ^= x << 17;\n    lamo_random_state = x;\n    return x;\n}\n\nstatic LAMO_UNUSED LamoValue lamo_random_int(LamoValue lo,"
    " LamoValue hi) {\n    long long low = lamo_as_int(lo);\n    long long high = lamo_as_int(hi);\n    long long range, r;\n"
    "    lamo_random_seed_if_needed();\n    if (high < low) { long long t = low; low = high; high = t; }\n    range = high - "
    "low + 1;\n    if (range <= 0) return lamo_make_int(low);\n    r = (long long)(lamo_random_next() % (unsigned long long)r"
    "ange);\n    return lamo_make_int(low + r);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_random_float(void) {\n    lamo_random"
    "_seed_if_needed();\n    /* Map to [0.0, 1.0). */\n    double v = (double)(lamo_random_next() >> 11) / (double)(1ULL << 5"
    "3);\n    return lamo_make_float(v);\n}\n\nstatic LAMO_UNUSED LamoValue lamo_random_bool(void) {\n    lamo_random_seed_if"
    "_needed();\n    return lamo_make_bool((int)(lamo_random_next() & 1));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_random_cho"
    "ice(LamoValue arr) {\n    LamoArray* a;\n    long long idx;\n    if (arr.type != LAMO_VALUE_ARRAY) {\n        lamo_runti"
    "me_type_error(\"random.choice: expected array\");\n    }\n    a = arr.array_value;\n    if (!a || a->count == 0) {\n    "
    "    lamo_runtime_type_error(\"random.choice: empty array\");\n    }\n    lamo_random_seed_if_needed();\n    idx = (long "
    "long)(lamo_random_next() % (unsigned long long)a->count);\n    return a->items[idx];\n}\n\nstatic LAMO_UNUSED LamoValue "
    "lamo_random_shuffle(LamoValue arr) {\n    /* Fisher-Yates shuffle, in place. */\n    LamoArray* a;\n    long long i;\n  "
    "  if (arr.type != LAMO_VALUE_ARRAY) {\n        lamo_runtime_type_error(\"random.shuffle: expected array\");\n    }\n    "
    "a = arr.array_value;\n    if (!a) return arr;\n    lamo_random_seed_if_needed();\n    for (i = a->count - 1; i > 0; i--)"
    " {\n        long long j = (long long)(lamo_random_next() % (unsigned long long)(i + 1));\n        LamoValue tmp = a->ite"
    "ms[i];\n        a->items[i] = a->items[j];\n        a->items[j] = tmp;\n    }\n    return arr;\n}\n\n/* ================"
    "================================================== */\n/* std.io \20024 additional I/O builtins                         "
    "           */\n/* ================================================================== */\n\nstatic LAMO_UNUSED LamoValue "
    "lamo_io_println(LamoValue v) {\n    lamo_print_value(v);\n    return lamo_make_int(0);\n}\n\nstatic LAMO_UNUSED LamoValu"
    "e lamo_io_eprint(LamoValue v) {\n    if (v.type == LAMO_VALUE_STRING) {\n        fprintf(stderr, \"%s\\n\", v.string_val"
    "ue ? v.string_value : \"\");\n    } else if (v.type == LAMO_VALUE_FLOAT) {\n        fprintf(stderr, \"%g\\n\", v.float_v"
    "alue);\n    } else {\n        fprintf(stderr, \"%lld\\n\", v.int_value);\n    }\n    return lamo_make_int(0);\n}\n\nstat"
    "ic LAMO_UNUSED LamoValue lamo_io_read_line(void) {\n    /* Read a line from stdin (without prompt). */\n    return lamo_"
    "input_str_value(lamo_make_string(\"\"));\n}\n\nstatic LAMO_UNUSED LamoValue lamo_io_write(LamoValue v) {\n    /* Print w"
    "ithout trailing newline. */\n    if (v.type == LAMO_VALUE_STRING) {\n        printf(\"%s\", v.string_value ? v.string_va"
    "lue : \"\");\n    } else if (v.type == LAMO_VALUE_FLOAT) {\n        printf(\"%g\", v.float_value);\n    } else if (v.typ"
    "e == LAMO_VALUE_BOOL) {\n        printf(\"%s\", v.int_value ? \"true\" : \"false\");\n    } else {\n        printf(\"%ll"
    "d\", v.int_value);\n    }\n    fflush(stdout);\n    return lamo_make_int(0);\n}\n\n/* =================================="
    "================================ */\n/* std.net \20024 HTTP client (simple, blocking)                            */\n/* "
    "================================================================== */\n\n/* http_get(url) -> string (response body, on s"
    "uccess; \"\" on failure). */\nstatic LAMO_UNUSED LamoValue lamo_net_http_get(LamoValue url_v) {\n    const char* url = l"
    "amo_as_cstring(url_v);\n    const char* host_start, *path_start, *port_start;\n    char host[256];\n    char path[2048];"
    "\n    int port = 80;\n    /* Parse: http://host[:port]/path */\n    if (strncmp(url, \"http://\", 7) != 0) {\n        fp"
    "rintf(stderr, \"runtime error: http_get: only http:// is supported (got '%s')\\n\", url);\n        return lamo_make_stri"
    "ng(\"\");\n    }\n    host_start = url + 7;\n    path_start = strchr(host_start, '/');\n    port_start = strchr(host_sta"
    "rt, ':');\n    if (port_start && (!path_start || port_start < path_start)) {\n        size_t host_len = (size_t)(port_st"
    "art - host_start);\n        if (host_len >= sizeof(host)) host_len = sizeof(host) - 1;\n        memcpy(host, host_start,"
    " host_len);\n        host[host_len] = '\\0';\n        port = atoi(port_start + 1);\n    } else {\n        size_t host_le"
    "n = path_start ? (size_t)(path_start - host_start) : strlen(host_start);\n        if (host_len >= sizeof(host)) host_len"
    " = sizeof(host) - 1;\n        memcpy(host, host_start, host_len);\n        host[host_len] = '\\0';\n    }\n    if (path_"
    "start) {\n        strncpy(path, path_start, sizeof(path) - 1);\n        path[sizeof(path) - 1] = '\\0';\n    } else {\n "
    "       strcpy(path, \"/\");\n    }\n    /* Connect. */\n    {\n#if defined(_WIN32)\n        WSADATA wsa;\n        SOCKET"
    " sock;\n        struct sockaddr_in addr;\n        struct hostent* he;\n        /* request buffer must fit: format (~72 c"
    "hars) + path (2048) + host (256).\n         * 4096 gives plenty of headroom and silences -Wformat-truncation. */\n      "
    "  char request[4096];\n        char* body;\n        if (WSAStartup(MAKEWORD(2,2), &wsa) != 0) return lamo_make_string(\""
    "\");\n        sock = socket(AF_INET, SOCK_STREAM, 0);\n        if (sock == INVALID_SOCKET) { WSACleanup(); return lamo_m"
    "ake_string(\"\"); }\n        he = gethostbyname(host);\n        if (!he) { closesocket(sock); WSACleanup(); return lamo_"
    "make_string(\"\"); }\n        addr.sin_family = AF_INET;\n        addr.sin_port = htons((unsigned short)port);\n        "
    "addr.sin_addr = *((struct in_addr*)he->h_addr);\n        if (connect(sock, (struct sockaddr*)&addr, sizeof(addr)) < 0) {"
    "\n            closesocket(sock); WSACleanup(); return lamo_make_string(\"\");\n        }\n        snprintf(request, size"
    "of(request),\n                 \"GET %s HTTP/1.1\\r\\nHost: %s\\r\\nConnection: close\\r\\nUser-Agent: lamo-http/1.0\\r"
    "\\n\\r\\n\",\n                 path, host);\n        send(sock, request, (int)strlen(request), 0);\n        /* Read resp"
    "onse into a heap-growing buffer. */\n        {\n            char* buf = NULL;\n            size_t buf_len = 0, buf_cap ="
    " 0;\n            char chunk[4096];\n            int chunk_n;\n            while ((chunk_n = recv(sock, chunk, sizeof(chu"
    "nk), 0)) > 0) {\n                if (buf_len + chunk_n + 1 > buf_cap) {\n                    while (buf_len + chunk_n + "
    "1 > buf_cap) buf_cap = buf_cap ? buf_cap * 2 : 8192;\n                    buf = (char*)realloc(buf, buf_cap);\n         "
    "       }\n                memcpy(buf + buf_len, chunk, chunk_n);\n                buf_len += chunk_n;\n            }\n  "
    "          closesocket(sock); WSACleanup();\n            if (!buf) return lamo_make_string(\"\");\n            buf[buf_le"
    "n] = '\\0';\n            /* Find body (after \\r\\n\\r\\n). */\n            body = strstr(buf, \"\\r\\n\\r\\n\");\n     "
    "       if (!body) { free(buf); return lamo_make_string(\"\"); }\n            body += 4;\n            {\n                "
    "LamoValue v = lamo_std_string(strdup(body));\n                free(buf);\n                return v;\n            }\n    "
    "    }\n#else\n        int sock;\n        struct sockaddr_in addr;\n        struct hostent* he;\n        /* request buffe"
    "r must fit: format (~72 chars) + path (2048) + host (256).\n         * 4096 gives plenty of headroom and silences -Wform"
    "at-truncation. */\n        char request[4096];\n        char chunk[4096];\n        char* buf = NULL;\n        size_t buf"
    "_len = 0, buf_cap = 0;\n        int chunk_n;\n        char* body;\n        sock = socket(AF_INET, SOCK_STREAM, 0);\n    "
    "    if (sock < 0) return lamo_make_string(\"\");\n        he = gethostbyname(host);\n        if (!he) { close(sock); ret"
    "urn lamo_make_string(\"\"); }\n        addr.sin_family = AF_INET;\n        addr.sin_port = htons((unsigned short)port);"
    "\n        addr.sin_addr = *((struct in_addr*)he->h_addr);\n        if (connect(sock, (struct sockaddr*)&addr, sizeof(add"
    "r)) < 0) {\n            close(sock); return lamo_make_string(\"\");\n        }\n        snprintf(request, sizeof(request"
    "),\n                 \"GET %s HTTP/1.1\\r\\nHost: %s\\r\\nConnection: close\\r\\nUser-Agent: lamo-http/1.0\\r\\n\\r\\n\""
    ",\n                 path, host);\n        send(sock, request, strlen(request), 0);\n        while ((chunk_n = recv(sock,"
    " chunk, sizeof(chunk), 0)) > 0) {\n            if (buf_len + chunk_n + 1 > buf_cap) {\n                while (buf_len + "
    "chunk_n + 1 > buf_cap) buf_cap = buf_cap ? buf_cap * 2 : 8192;\n                buf = (char*)realloc(buf, buf_cap);\n   "
    "         }\n            memcpy(buf + buf_len, chunk, chunk_n);\n            buf_len += chunk_n;\n        }\n        clos"
    "e(sock);\n        if (!buf) return lamo_make_string(\"\");\n        buf[buf_len] = '\\0';\n        body = strstr(buf, \""
    "\\r\\n\\r\\n\");\n        if (!body) { free(buf); return lamo_make_string(\"\"); }\n        body += 4;\n        {\n     "
    "       LamoValue v = lamo_std_string(strdup(body));\n            free(buf);\n            return v;\n        }\n#endif\n "
    "   }\n}\n\n/* http_post(url, body) -> string (response body). */\nstatic LAMO_UNUSED LamoValue lamo_net_http_post(LamoVa"
    "lue url_v, LamoValue body_v) {\n    const char* url = lamo_as_cstring(url_v);\n    const char* body = lamo_as_cstring(bo"
    "dy_v);\n    char host[256];\n    char path[2048];\n    int port = 80;\n    const char* host_start, *path_start, *port_st"
    "art;\n    if (strncmp(url, \"http://\", 7) != 0) {\n        fprintf(stderr, \"runtime error: http_post: only http:// is "
    "supported\\n\");\n        return lamo_make_string(\"\");\n    }\n    host_start = url + 7;\n    path_start = strchr(host"
    "_start, '/');\n    port_start = strchr(host_start, ':');\n    if (port_start && (!path_start || port_start < path_start)"
    ") {\n        size_t host_len = (size_t)(port_start - host_start);\n        if (host_len >= sizeof(host)) host_len = size"
    "of(host) - 1;\n        memcpy(host, host_start, host_len);\n        host[host_len] = '\\0';\n        port = atoi(port_st"
    "art + 1);\n    } else {\n        size_t host_len = path_start ? (size_t)(path_start - host_start) : strlen(host_start);"
    "\n        if (host_len >= sizeof(host)) host_len = sizeof(host) - 1;\n        memcpy(host, host_start, host_len);\n     "
    "   host[host_len] = '\\0';\n    }\n    if (path_start) {\n        strncpy(path, path_start, sizeof(path) - 1);\n        "
    "path[sizeof(path) - 1] = '\\0';\n    } else {\n        strcpy(path, \"/\");\n    }\n    {\n#if defined(_WIN32)\n        "
    "/* Similar to GET but with POST and Content-Length. */\n        WSADATA wsa;\n        SOCKET sock;\n        struct socka"
    "ddr_in addr;\n        struct hostent* he;\n        char request[4096];\n        if (WSAStartup(MAKEWORD(2,2), &wsa) != 0"
    ") return lamo_make_string(\"\");\n        sock = socket(AF_INET, SOCK_STREAM, 0);\n        if (sock == INVALID_SOCKET) {"
    " WSACleanup(); return lamo_make_string(\"\"); }\n        he = gethostbyname(host);\n        if (!he) { closesocket(sock)"
    "; WSACleanup(); return lamo_make_string(\"\"); }\n        addr.sin_family = AF_INET;\n        addr.sin_port = htons((uns"
    "igned short)port);\n        addr.sin_addr = *((struct in_addr*)he->h_addr);\n        if (connect(sock, (struct sockaddr*"
    ")&addr, sizeof(addr)) < 0) {\n            closesocket(sock); WSACleanup(); return lamo_make_string(\"\");\n        }\n  "
    "      snprintf(request, sizeof(request),\n                 \"POST %s HTTP/1.1\\r\\nHost: %s\\r\\nContent-Type: text/plai"
    "n\\r\\nContent-Length: %zu\\r\\nConnection: close\\r\\nUser-Agent: lamo-http/1.0\\r\\n\\r\\n%s\",\n                 path"
    ", host, strlen(body), body);\n        send(sock, request, (int)strlen(request), 0);\n        {\n            char* buf = "
    "NULL;\n            size_t buf_len = 0, buf_cap = 0;\n            char chunk[4096];\n            int chunk_n;\n          "
    "  char* resp_body;\n            while ((chunk_n = recv(sock, chunk, sizeof(chunk), 0)) > 0) {\n                if (buf_l"
    "en + chunk_n + 1 > buf_cap) {\n                    while (buf_len + chunk_n + 1 > buf_cap) buf_cap = buf_cap ? buf_cap *"
    " 2 : 8192;\n                    buf = (char*)realloc(buf, buf_cap);\n                }\n                memcpy(buf + buf"
    "_len, chunk, chunk_n);\n                buf_len += chunk_n;\n            }\n            closesocket(sock); WSACleanup();"
    "\n            if (!buf) return lamo_make_string(\"\");\n            buf[buf_len] = '\\0';\n            resp_body = strst"
    "r(buf, \"\\r\\n\\r\\n\");\n            if (!resp_body) { free(buf); return lamo_make_string(\"\"); }\n            resp_b"
    "ody += 4;\n            {\n                LamoValue v = lamo_std_string(strdup(resp_body));\n                free(buf);"
    "\n                return v;\n            }\n        }\n#else\n        int sock;\n        struct sockaddr_in addr;\n     "
    "   struct hostent* he;\n        char* request = NULL;\n        size_t req_len;\n        char* buf = NULL;\n        size_"
    "t buf_len = 0, buf_cap = 0;\n        char chunk[4096];\n        int chunk_n;\n        char* resp_body;\n        sock = s"
    "ocket(AF_INET, SOCK_STREAM, 0);\n        if (sock < 0) return lamo_make_string(\"\");\n        he = gethostbyname(host);"
    "\n        if (!he) { close(sock); return lamo_make_string(\"\"); }\n        addr.sin_family = AF_INET;\n        addr.sin"
    "_port = htons((unsigned short)port);\n        addr.sin_addr = *((struct in_addr*)he->h_addr);\n        if (connect(sock,"
    " (struct sockaddr*)&addr, sizeof(addr)) < 0) {\n            close(sock); return lamo_make_string(\"\");\n        }\n    "
    "    req_len = strlen(body) + 1024;\n        request = (char*)malloc(req_len);\n        snprintf(request, req_len,\n     "
    "            \"POST %s HTTP/1.1\\r\\nHost: %s\\r\\nContent-Type: text/plain\\r\\nContent-Length: %zu\\r\\nConnection: clo"
    "se\\r\\nUser-Agent: lamo-http/1.0\\r\\n\\r\\n%s\",\n                 path, host, strlen(body), body);\n        send(sock"
    ", request, strlen(request), 0);\n        free(request);\n        while ((chunk_n = recv(sock, chunk, sizeof(chunk), 0)) "
    "> 0) {\n            if (buf_len + chunk_n + 1 > buf_cap) {\n                while (buf_len + chunk_n + 1 > buf_cap) buf_"
    "cap = buf_cap ? buf_cap * 2 : 8192;\n                buf = (char*)realloc(buf, buf_cap);\n            }\n            mem"
    "cpy(buf + buf_len, chunk, chunk_n);\n            buf_len += chunk_n;\n        }\n        close(sock);\n        if (!buf)"
    " return lamo_make_string(\"\");\n        buf[buf_len] = '\\0';\n        resp_body = strstr(buf, \"\\r\\n\\r\\n\");\n    "
    "    if (!resp_body) { free(buf); return lamo_make_string(\"\"); }\n        resp_body += 4;\n        {\n            LamoV"
    "alue v = lamo_std_string(strdup(resp_body));\n            free(buf);\n            return v;\n        }\n#endif\n    }\n}"
    "\n\n#endif /* LAMO_NEEDS_STD_RUNTIME */\n\n#endif /* LAMO_RUNTIME_H */\n"
;
